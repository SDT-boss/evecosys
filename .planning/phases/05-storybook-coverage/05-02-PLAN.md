---
phase: 05-storybook-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/layout/shell/ContentStates.tsx
  - components/board/settings/InviteStateRow.tsx
  - components/board/settings/ResendConfirm.tsx
  - components/board/settings/AccessDenied.tsx
  - test/unit/components/shell/ContentStates.test.tsx
  - test/unit/components/board/settings/InviteStateRow.test.tsx
autonomous: true
requirements:
  - STRB-01

must_haves:
  truths:
    - "All five content states (LoadingState, EmptyState, ErrorState, RestrictedState, UnavailableState) are exported from ContentStates.tsx"
    - "InviteStateRow renders all 5 lifecycle states driven by props only (no DB query)"
    - "InviteStateRow with isLimitedAccess=true shows lock glyph and disabled buttons, NOT hidden controls"
    - "AccessDenied renders 72px lock circle in #fce4e4 with role-specific copy"
    - "make typecheck passes; make lint passes"
  artifacts:
    - path: "components/layout/shell/ContentStates.tsx"
      provides: "LoadingState, EmptyState, ErrorState, RestrictedState, UnavailableState, Skel"
      exports: ["LoadingState", "EmptyState", "ErrorState", "RestrictedState", "UnavailableState", "Skel"]
    - path: "components/board/settings/InviteStateRow.tsx"
      provides: "Invite lifecycle row with 5 states + LimitedAccess variant"
      exports: ["InviteStateRow", "InviteState", "InviteStateRowProps"]
    - path: "components/board/settings/AccessDenied.tsx"
      provides: "403 / access denied screen"
      exports: ["AccessDenied"]
    - path: "test/unit/components/shell/ContentStates.test.tsx"
      provides: "Unit tests for all 5 content states"
    - path: "test/unit/components/board/settings/InviteStateRow.test.tsx"
      provides: "Unit tests for 5 lifecycle states + LimitedAccess variant"
  key_links:
    - from: "components/board/settings/InviteStateRow.tsx"
      to: "components/board/settings/ResendConfirm.tsx"
      via: "renders ResendConfirm dialog on Resend button click"
---

<objective>
Build all shell content state components and the RBAC invite flow components. These are net-new components with no live data dependency — all driven by props. No layout files are modified in this plan. Runs in parallel with Plan 01 (no file overlap).

Purpose: Provides the content state and RBAC components needed by Plan 03 (layout migration + error.tsx wiring) and Plan 04/05 (Storybook stories).
Output: ContentStates.tsx, InviteStateRow.tsx, ResendConfirm.tsx, AccessDenied.tsx + unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-storybook-coverage/05-CONTEXT.md
@.planning/phases/05-storybook-coverage/05-RESEARCH.md
@components/board/settings/MemberTable.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build ContentStates (5 shell states + Skel shimmer)</name>
  <files>
    components/layout/shell/ContentStates.tsx,
    test/unit/components/shell/ContentStates.test.tsx
  </files>
  <read_first>
    1. DesignSync get_file — projectId "35effb2c-c297-426f-b553-ac4ed7e8cee6", path "screens/shell/shell-states.tsx" — read BEFORE writing any component
    2. DesignSync get_file — projectId "35effb2c-c297-426f-b553-ac4ed7e8cee6", path "colors_and_type.css" — token reference
  </read_first>
  <behavior>
    - LoadingState test: renders shimmer grid using Skel component; no text content rendered
    - EmptyState test: given icon and message props, renders the icon and message text
    - EmptyState test: given ctaLabel and onCta props, renders a button with ctaLabel text
    - EmptyState test: given no ctaLabel, renders no button
    - ErrorState test: renders "cloud_off" icon (or Material symbol equivalent) and Retry + Report issue buttons
    - RestrictedState test: renders "403" and "Restricted" text and Back + Request access buttons
    - UnavailableState test: renders Upgrade plan + See what's included buttons
  </behavior>
  <action>
    Write test file first. Mock no external dependencies — all components are pure presentational RSC-compatible components.

    Then create components/layout/shell/ContentStates.tsx exporting:

    Skel: a div with inline shimmer animation. Colors per D-04: background linear-gradient from #eef2ee to #f6f9f6. // raw hex — no --ds-* token for card border per CONTEXT.md D-03

    LoadingState: renders a shimmer grid using multiple Skel components arranged in a table-like layout.

    EmptyState: props = { icon?: React.ReactNode; message: string; ctaLabel?: string; onCta?: () => void }. Renders 64px square icon container with background #f2f5f2, message, optional CTA via Btn (use the existing Button from @evecosys/design-system).

    ErrorState: icon circle with red background #fdeaea, border #f5c0c0; renders two buttons: "Retry" (calls onRetry?: () => void) and "Report issue" (calls onReport?: () => void).

    RestrictedState: icon circle with red background (same as ErrorState); renders "403 · Restricted" heading; renders "Back to Overview" button (calls onBack?: () => void) and "Request access" button (calls onRequestAccess?: () => void).

    UnavailableState: purple circle #efe7fb, border #d6c4f0; renders "Upgrade plan" button and "See what's included" link button.

    All color values that have no --ds-* equivalent: use raw hex. Use var(--ds-color-brand-primary) for interactive elements (links, focus rings). Use var(--ds-font-family-sans) for typography.
  </action>
  <verify>
    <automated>cd /Users/shannendorothee/Projects/evecosys && npx vitest run test/unit/components/shell/ContentStates.test.tsx --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>
    ContentStates.tsx exports Skel, LoadingState, EmptyState, ErrorState, RestrictedState, UnavailableState. All unit tests pass. make typecheck exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build InviteStateRow, ResendConfirm, and AccessDenied</name>
  <files>
    components/board/settings/InviteStateRow.tsx,
    components/board/settings/ResendConfirm.tsx,
    components/board/settings/AccessDenied.tsx,
    test/unit/components/board/settings/InviteStateRow.test.tsx
  </files>
  <read_first>
    1. DesignSync get_file — projectId "35effb2c-c297-426f-b553-ac4ed7e8cee6", path "screens/rbac/screens-states.tsx" — read BEFORE writing
    2. DesignSync get_file — projectId "35effb2c-c297-426f-b553-ac4ed7e8cee6", path "screens/rbac/kit.tsx" — primitive components
    3. DesignSync get_file — projectId "35effb2c-c297-426f-b553-ac4ed7e8cee6", path "screens/rbac/screens-users.tsx" — user screen context
    4. components/board/settings/MemberTable.tsx — understand existing member row pattern to align styles
  </read_first>
  <behavior>
    - InviteStateRow — Sent state: renders amber badge; renders "Expires in N days" text; renders Resend + Revoke buttons
    - InviteStateRow — Expiring state: renders amber badge; renders "Expires in NN hours" text; renders Resend + Revoke buttons
    - InviteStateRow — Accepted state: renders green badge; renders "Joined" text; renders View button only (no Resend/Revoke)
    - InviteStateRow — Expired state: renders grey badge; renders "Expired N days ago" text; renders Re-invite button
    - InviteStateRow — Revoked state: renders red badge; renders "Revoked" text; renders Re-invite button
    - InviteStateRow — isLimitedAccess=true: renders all action buttons as DISABLED with a lock glyph; does NOT hide them
  </behavior>
  <action>
    Write InviteStateRow.test.tsx first with all 6 test cases (5 states + LimitedAccess variant). No external mocks needed — pure prop-driven component.

    InviteStateRow props type (export as InviteStateRowProps):
    - state: 'sent' | 'expiring' | 'accepted' | 'expired' | 'revoked'  (export as InviteState)
    - email: string
    - name?: string
    - expiresAt?: Date (for sent/expiring states)
    - joinedAt?: Date (for accepted state)
    - revokedAt?: Date; revokedBy?: string (for revoked state)
    - onResend?: () => void
    - onRevoke?: () => void
    - onView?: () => void
    - onReinvite?: () => void
    - isLimitedAccess?: boolean

    Badge colors: amber = #f59e0b background with dark text, green = var(--ds-color-brand-primary) background, grey = #6b7280, red = #ef4444. Use inline styles or Tailwind arbitrary values.

    LimitedAccess: wrap each action button with a span containing a Lock icon (lucide-react Lock) when isLimitedAccess is true. Set disabled={true} on the button. DO NOT hide the button.

    ResendConfirm: a 'use client' dialog component using AlertDialog from @evecosys/design-system (or standard Dialog if AlertDialog not available). On confirmation, calls onConfirm: () => void. On success, shows a toast-like notification with background #1a1a1a, checkmark color #7cc242, and an "Undo" link. The toast is a small absolute-positioned div within ResendConfirm — not a global toast system. Props: open: boolean; onClose: () => void; onConfirm: () => void; success?: boolean; onUndo?: () => void.

    AccessDenied: pure presentational. Props: role: string; onBack?: () => void; onRequestAccess?: () => void. Renders 72px circle with background #fce4e4, Lock icon (lucide-react), "403 · Access denied" heading, role-specific copy ("Your {role} role does not have access to this area"), Back button, Request access button.
  </action>
  <verify>
    <automated>cd /Users/shannendorothee/Projects/evecosys && npx vitest run test/unit/components/board/settings/InviteStateRow.test.tsx --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>
    InviteStateRow.tsx, ResendConfirm.tsx, AccessDenied.tsx all exist and export their named components. All InviteStateRow tests pass. isLimitedAccess variant renders buttons disabled with lock glyph. make typecheck exits 0. make lint exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| InviteStateRow props | State data comes from parent; no DB access in this component |
| AccessDenied role prop | Role string comes from authenticated session via parent RSC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Information Disclosure | InviteStateRow | accept | component is prop-driven display only; no data fetching |
| T-05-SC | Tampering | npm installs | accept | no new packages in this plan; lucide-react and @evecosys/design-system already in dependencies |
</threat_model>

<verification>
cd /Users/shannendorothee/Projects/evecosys && make typecheck && make lint && npx vitest run test/unit/components/shell/ContentStates.test.tsx test/unit/components/board/settings/InviteStateRow.test.tsx
</verification>

<success_criteria>
- components/layout/shell/ContentStates.tsx exports Skel, LoadingState, EmptyState, ErrorState, RestrictedState, UnavailableState
- components/board/settings/InviteStateRow.tsx exports InviteStateRow, InviteState type, InviteStateRowProps type
- components/board/settings/ResendConfirm.tsx exports ResendConfirm
- components/board/settings/AccessDenied.tsx exports AccessDenied
- InviteStateRow with isLimitedAccess=true: buttons are disabled and show lock glyph, NOT hidden
- AccessDenied: circle has background #fce4e4
- All ContentStates unit tests pass
- All InviteStateRow unit tests pass
- make typecheck exits 0
- make lint exits 0
</success_criteria>

<output>
Create `.planning/phases/05-storybook-coverage/05-02-SUMMARY.md` when done
</output>
