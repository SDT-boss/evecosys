---
phase: 05-storybook-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/layout/LeftRailShell.tsx
  - components/layout/shell/BrandHeader.tsx
  - components/layout/shell/SidebarSearch.tsx
  - components/layout/shell/NavItem.tsx
  - components/layout/shell/AccountBlock.tsx
  - components/layout/shell/AskEveLauncher.tsx
  - components/layout/shell/ContentUtilityBar.tsx
  - components/layout/shell/TenantSwitcher.tsx
  - test/unit/components/shell/LeftRailShell.test.tsx
autonomous: true
requirements:
  - STRB-01

must_haves:
  truths:
    - "LeftRailShell component exists and accepts navItems, user, and optional alertBell props"
    - "TenantSwitcher sidebar dropdown component exists as a standalone component"
    - "AskEveLauncher renders as an inert placeholder button"
    - "make typecheck passes with no errors in the new shell files"
    - "make lint passes with no errors in the new shell files"
  artifacts:
    - path: "components/layout/LeftRailShell.tsx"
      provides: "New left-rail shell wrapper — replaces DashboardShell"
      exports: ["LeftRailShell", "LeftRailShellProps"]
    - path: "components/layout/shell/TenantSwitcher.tsx"
      provides: "Sidebar tenant dropdown (distinct from TenantList table)"
      exports: ["TenantSwitcher"]
    - path: "components/layout/shell/ContentUtilityBar.tsx"
      provides: "Content-area header bar with LIVE chip, alertBell slot"
      exports: ["ContentUtilityBar"]
    - path: "test/unit/components/shell/LeftRailShell.test.tsx"
      provides: "Unit tests — nav items render, active state via mocked usePathname"
  key_links:
    - from: "components/layout/LeftRailShell.tsx"
      to: "components/layout/shell/NavItem.tsx"
      via: "renders nav items from navItems prop"
    - from: "components/layout/shell/TenantSwitcher.tsx"
      to: "components/platform/TenantContext.tsx"
      via: "useTenantContext() for active tenant name"
---

<objective>
Build the new LeftRailShell component and all its sub-components from the DesignSync spec. No layout consumer files are changed yet — this plan creates the new components in isolation so Wave 2 can rewire layouts against stable contracts.

Purpose: Establish the component contracts that the shell migration (Plan 02) and Storybook stories (Plan 04/05) depend on.
Output: LeftRailShell.tsx + 7 sub-components + unit test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-storybook-coverage/05-CONTEXT.md
@.planning/phases/05-storybook-coverage/05-RESEARCH.md
@components/layout/DashboardShell.tsx
@components/platform/TenantContext.tsx
@components/platform/ActiveTenantIndicator.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build LeftRailShell sub-components from DesignSync spec</name>
  <files>
    components/layout/shell/BrandHeader.tsx,
    components/layout/shell/SidebarSearch.tsx,
    components/layout/shell/NavItem.tsx,
    components/layout/shell/AccountBlock.tsx,
    components/layout/shell/AskEveLauncher.tsx,
    components/layout/shell/ContentUtilityBar.tsx,
    components/layout/shell/TenantSwitcher.tsx
  </files>
  <read_first>
    1. DesignSync get_file — projectId "35effb2c-c297-426f-b553-ac4ed7e8cee6", path "screens/shell/shell-kit.tsx" — read BEFORE writing any component
    2. DesignSync get_file — projectId "35effb2c-c297-426f-b553-ac4ed7e8cee6", path "screens/shell/shell-data.tsx" — nav structure
    3. DesignSync get_file — projectId "35effb2c-c297-426f-b553-ac4ed7e8cee6", path "colors_and_type.css" — token reference
    4. components/layout/DashboardShell.tsx — understand what is being replaced
    5. components/platform/TenantContext.tsx — understand useTenantContext() shape
  </read_first>
  <behavior>
    - BrandHeader: renders EVEcosys logo/wordmark; accepts collapsed: boolean prop to show only mark when collapsed
    - SidebarSearch: renders a search input inside the rail; accepts placeholder: string; no-op onSearch in this phase
    - NavItem: accepts href, label, icon (React node), active: boolean; renders with active bg rgba(0,134,132,0.08) and text/icon color var(--ds-color-brand-primary) when active; NOT a string icon name
    - AccountBlock: accepts user: { name: string, email: string, avatarUrl?: string }; renders avatar + name + logout button
    - AskEveLauncher: inert placeholder button — renders visually but onClick is a no-op; clear code comment: "AskEVE AI integration deferred to separate milestone"
    - ContentUtilityBar: accepts children (alertBell slot) + optional inviteCta: React.ReactNode; renders LIVE chip + slot for notifications
    - TenantSwitcher: sidebar dropdown variant (NOT the TenantList table); reads active tenant from useTenantContext(); renders collapsed state (just name + chevron) and expanded state (dropdown list); accepts tenants: Array&lt;{id: string, name: string}&gt; + onSelect: (id: string) =&gt; void
    - ALL components: use var(--ds-*) tokens for colors/spacing/radii per CLAUDE.md; rail bg #eef3f0 and page ground #f2f5f2 are raw values (no --ds-* token exists)
    - DO NOT use --ev-* tokens on new surfaces; DO NOT delete --ev-* tokens
  </behavior>
  <action>
    Call DesignSync get_file for all three files listed in read_first before writing any code. Extract exact component structure, prop signatures, and layout from the spec.

    Create components/layout/shell/ directory. Build each sub-component as a named export in its own file. Use 'use client' directive only on components that need hooks (TenantSwitcher uses useTenantContext, NavItem uses usePathname for active detection, AccountBlock uses useRouter for logout).

    NavItem: import usePathname from next/navigation; derive active from pathname === href rather than accepting an active prop — this makes it self-aware without needing the parent to pass active state.

    TenantSwitcher: import useTenantContext from @/components/platform/TenantContext. Accept tenants prop and onSelect prop to avoid direct Server Action calls (making it story-friendly). Use Radix Popover or a simple &lt;details&gt; for the dropdown — check DesignSync spec for which pattern is used.

    AskEveLauncher: single &lt;button&gt; with the icon from the spec, onClick={() =&gt; {}} with the deferred comment.

    ContentUtilityBar: RSC-compatible (no 'use client'); render a LIVE chip using var(--ds-color-brand-secondary) background (Volt Green per D-03), plus children slot.
  </action>
  <verify>
    <automated>cd /Users/shannendorothee/Projects/evecosys && make typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>
    All 7 files exist under components/layout/shell/. make typecheck exits 0. No --ev-* usage on new surfaces. NavItem active state uses var(--ds-color-brand-primary). AskEveLauncher has deferred comment.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build LeftRailShell wrapper + unit tests</name>
  <files>
    components/layout/LeftRailShell.tsx,
    test/unit/components/shell/LeftRailShell.test.tsx
  </files>
  <read_first>
    1. components/layout/DashboardShell.tsx — full file; understand current props interface and children pattern
    2. components/layout/shell/NavItem.tsx — just created in Task 1; understand import path
    3. components/layout/shell/AccountBlock.tsx — just created; understand user prop shape
    4. components/layout/AlertBellWrapper.tsx — understand what it exports so ContentUtilityBar can slot it
    5. design-system/stories/DashboardShell.stories.tsx — lines 1-40; understand existing story pattern to know what the replacement must support
  </read_first>
  <behavior>
    - LeftRailShell test — nav item renders: given navItems with href "/board", renders an anchor with href "/board"
    - LeftRailShell test — active item: with mocked usePathname returning "/board", NavItem for "/board" has active class or style indicator containing rgba(0,134,132,0.08)
    - LeftRailShell test — children render: children prop content appears in the output
    - LeftRailShell test — alertBell slot: alertBell prop content appears inside ContentUtilityBar region
  </behavior>
  <action>
    Write test file first using vitest + @testing-library/react. Mock next/navigation to control usePathname return value. Mock @/components/platform/TenantContext to return a minimal tenant context. Run tests — they MUST fail (RED).

    Then create components/layout/LeftRailShell.tsx as a 'use client' component. Props interface:
    - navItems: Array&lt;{ label: string; icon: React.ReactNode; href: string }&gt;
    - user: AppUser
    - alertBell?: React.ReactNode
    - children: React.ReactNode

    Layout: fixed left rail (264px expanded / 72px collapsed via useState), content area to the right. Rail renders: BrandHeader, AskEveLauncher, SidebarSearch, TenantSwitcher (only for platform admin — check user.role, render null for other roles), divider, NavItem list from navItems, AccountBlock. Above children in the content area, render ContentUtilityBar with alertBell prop.

    Import all sub-components from ./shell/. Import AppUser from @/types. Do NOT import from DashboardShell.tsx.

    Run tests after implementation — they MUST pass (GREEN).
  </action>
  <verify>
    <automated>cd /Users/shannendorothee/Projects/evecosys && npx vitest run test/unit/components/shell/LeftRailShell.test.tsx --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>
    components/layout/LeftRailShell.tsx exists and exports LeftRailShell. All tests in LeftRailShell.test.tsx pass. make typecheck exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User prop → AccountBlock | user object flows from authenticated RSC layout; no re-validation needed here |
| TenantSwitcher onSelect | calls parent-provided callback; parent is responsible for action dispatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | TenantSwitcher | accept | tenants prop populated by parent RSC after auth check; TenantSwitcher is display-only |
| T-05-SC | Tampering | npm installs | accept | no new packages added in this plan; all imports are from existing dependencies |
</threat_model>

<verification>
cd /Users/shannendorothee/Projects/evecosys && make typecheck && make lint && npx vitest run test/unit/components/shell/LeftRailShell.test.tsx
</verification>

<success_criteria>
- components/layout/LeftRailShell.tsx exists with LeftRailShellProps interface exported
- components/layout/shell/ directory contains: BrandHeader.tsx, SidebarSearch.tsx, NavItem.tsx, AccountBlock.tsx, AskEveLauncher.tsx, ContentUtilityBar.tsx, TenantSwitcher.tsx
- AskEveLauncher.tsx contains the deferred comment about AI integration
- No --ev-* token usage in any new shell file
- make typecheck exits 0
- make lint exits 0
- All LeftRailShell unit tests pass
</success_criteria>

<output>
Create `.planning/phases/05-storybook-coverage/05-01-SUMMARY.md` when done
</output>
