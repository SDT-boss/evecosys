---
phase: 05-storybook-coverage
plan: 05
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
  - "05-03"
files_modified:
  - design-system/stories/ContentStates.stories.tsx
  - design-system/stories/InviteStateRow.stories.tsx
autonomous: true
requirements:
  - STRB-01

must_haves:
  truths:
    - "ContentStates.stories.tsx covers all 5 shell states: LoadingState, EmptyState, ErrorState, RestrictedState, UnavailableState"
    - "InviteStateRow.stories.tsx covers all 5 lifecycle states + LimitedAccess variant = 6 stories"
    - "npm run build-storybook exits 0 with all 5 new story files present"
    - "make check exits 0 (all 5 CI jobs green)"
  artifacts:
    - path: "design-system/stories/ContentStates.stories.tsx"
      provides: "5 content state stories"
      exports: ["Loading", "Empty", "EmptyWithCta", "Error", "Restricted", "Unavailable"]
    - path: "design-system/stories/InviteStateRow.stories.tsx"
      provides: "6 InviteStateRow stories (5 states + LimitedAccess)"
      exports: ["Sent", "Expiring", "Accepted", "Expired", "Revoked", "LimitedAccess"]
  key_links:
    - from: "design-system/stories/ContentStates.stories.tsx"
      to: "components/layout/shell/ContentStates.tsx"
      via: "direct import of named exports"
    - from: "design-system/stories/InviteStateRow.stories.tsx"
      to: "components/board/settings/InviteStateRow.tsx"
      via: "direct import; all states driven by story args"
---

<objective>
Write Storybook stories for the shell content states and InviteStateRow lifecycle states built in Plan 02. This plan runs in parallel with Plan 04 (Wave 3, no file overlap).

Purpose: Covers the remaining new components with stories, completing STRB-01 coverage.
Output: 2 story files covering 11 additional story variants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-storybook-coverage/05-CONTEXT.md
@.planning/phases/05-storybook-coverage/05-02-SUMMARY.md
@.planning/phases/05-storybook-coverage/05-03-SUMMARY.md
@design-system/stories/Badge.stories.tsx
</context>

<interfaces>
<!-- Key patterns from existing stories. Executor does not need to re-read these. -->

Story file pattern (from Badge.stories.tsx):
```typescript
import type { Meta, StoryObj } from "@storybook/react";
const meta: Meta<typeof Comp> = { title: "Category/Name", component: Comp };
export default meta;
type Story = StoryObj<typeof Comp>;
export const StoryName: Story = { args: { ... } };
```

InviteState type (from 05-02-SUMMARY.md):
```typescript
type InviteState = 'sent' | 'expiring' | 'accepted' | 'expired' | 'revoked'
```

InviteStateRowProps shape (key fields for stories):
- state: InviteState
- email: string
- name?: string
- expiresAt?: Date
- joinedAt?: Date
- revokedAt?: Date; revokedBy?: string
- onResend?: () => void; onRevoke?: () => void; onView?: () => void; onReinvite?: () => void
- isLimitedAccess?: boolean
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ContentStates stories (all 5 shell states)</name>
  <files>
    design-system/stories/ContentStates.stories.tsx
  </files>
  <read_first>
    1. components/layout/shell/ContentStates.tsx — from 05-02-SUMMARY.md; understand exact named exports and prop interfaces
    2. design-system/stories/Badge.stories.tsx — lines 1-30; pattern reference for Meta + StoryObj
  </read_first>
  <action>
    Create design-system/stories/ContentStates.stories.tsx.

    Since the file covers multiple components (not a single component), use a "module-level" pattern: export each story using the appropriate component. This requires multiple meta-less component exports, OR use LoadingState as the primary Meta component and export others via render functions. The cleanest approach: use LoadingState as the default meta component, and for each other state create a Story with render: () => &lt;Component /&gt;.

    Meta: title "Components/ContentStates", component LoadingState. No decorators needed — all components are pure presentational with no context dependencies.

    Stories:
    1. Loading: Story = { render: () => &lt;LoadingState /&gt; } — shows shimmer grid
    2. Empty: Story = { render: () => &lt;EmptyState message="No vehicles found" /&gt; }
    3. EmptyWithCta: Story = { render: () => &lt;EmptyState message="No vehicles found" ctaLabel="Add your first vehicle" onCta={fn()} /&gt; }
    4. Error: Story = { render: () => &lt;ErrorState onRetry={fn()} onReport={fn()} /&gt; }
    5. Restricted: Story = { render: () => &lt;RestrictedState onBack={fn()} onRequestAccess={fn()} /&gt; }
    6. Unavailable: Story = { render: () => &lt;UnavailableState onUpgrade={fn()} onSeeFeatures={fn()} /&gt; }

    Import: LoadingState, EmptyState, ErrorState, RestrictedState, UnavailableState from @/components/layout/shell/ContentStates. Import fn from @storybook/test.

    All stories should render in a centered container — add a decorator that wraps in a div with max-width 600px and padding 24px to provide realistic viewport context.
  </action>
  <verify>
    <automated>cd /Users/shannendorothee/Projects/evecosys && npm run build-storybook 2>&1 | grep -E "(error|Error|FAIL|✓|✗)" | head -20</automated>
  </verify>
  <done>
    design-system/stories/ContentStates.stories.tsx exists. Exports Loading, Empty, EmptyWithCta, Error, Restricted, Unavailable stories. npm run build-storybook exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: InviteStateRow stories (5 lifecycle states + LimitedAccess)</name>
  <files>
    design-system/stories/InviteStateRow.stories.tsx
  </files>
  <read_first>
    1. components/board/settings/InviteStateRow.tsx — from 05-02-SUMMARY.md; understand exact props, badge colors, and LimitedAccess behavior
    2. design-system/stories/Badge.stories.tsx — lines 1-30; story pattern reference
  </read_first>
  <action>
    Create design-system/stories/InviteStateRow.stories.tsx.

    Meta: title "Components/InviteStateRow", component InviteStateRow. Import InviteStateRow from @/components/board/settings/InviteStateRow. Import fn from @storybook/test.

    Stories (each uses args pattern since InviteStateRow is fully prop-driven):

    1. Sent: args = { state: 'sent', email: 'charlie@example.com', name: 'Charlie', expiresAt: new Date(Date.now() + 5 * 24 * 60 * 60 * 1000), onResend: fn(), onRevoke: fn() }
       — should show amber badge, "Expires in 5 days", Resend + Revoke buttons

    2. Expiring: args = { state: 'expiring', email: 'dana@example.com', name: 'Dana', expiresAt: new Date(Date.now() + 8 * 60 * 60 * 1000), onResend: fn(), onRevoke: fn() }
       — should show amber badge, "Expires in 8 hours", Resend + Revoke buttons

    3. Accepted: args = { state: 'accepted', email: 'evan@example.com', name: 'Evan', joinedAt: new Date(Date.now() - 2 * 60 * 60 * 1000), onView: fn() }
       — should show green badge, "Joined · last active 2h ago", View button only

    4. Expired: args = { state: 'expired', email: 'fiona@example.com', name: 'Fiona', expiresAt: new Date(Date.now() - 3 * 24 * 60 * 60 * 1000), onReinvite: fn() }
       — should show grey badge, "Expired 3 days ago", Re-invite button

    5. Revoked: args = { state: 'revoked', email: 'george@example.com', name: 'George', revokedAt: new Date(Date.now() - 1 * 24 * 60 * 60 * 1000), revokedBy: 'Admin User', onReinvite: fn() }
       — should show red badge, "Revoked 01 Jun by Admin User", Re-invite button

    6. LimitedAccess: args = { state: 'sent', email: 'helen@example.com', name: 'Helen', expiresAt: new Date(Date.now() + 2 * 24 * 60 * 60 * 1000), onResend: fn(), onRevoke: fn(), isLimitedAccess: true }
       — buttons visible but disabled, lock glyph visible alongside each button

    Add a decorator wrapping in a white card container with border 1px solid #eef2ee and padding 16px to match how InviteStateRow appears in the users page context.
  </action>
  <verify>
    <automated>cd /Users/shannendorothee/Projects/evecosys && npm run build-storybook 2>&1 | tail -10</automated>
  </verify>
  <done>
    design-system/stories/InviteStateRow.stories.tsx exports Sent, Expiring, Accepted, Expired, Revoked, LimitedAccess stories. npm run build-storybook exits 0. All story args use static Date values (not relative calculations that could break SSR).
  </done>
</task>

<task type="auto">
  <name>Task 3: Phase gate — full CI check + Storybook build</name>
  <files></files>
  <read_first>
    No files to read — this is a validation task only.
  </read_first>
  <action>
    Run the complete CI check suite and Storybook build to confirm phase 5 is complete:
    1. make check (runs lint + typecheck + test + tokens + build in parallel)
    2. npm run build-storybook

    If make check fails, identify which CI job failed and fix before proceeding.
    If build-storybook fails, identify which story import is broken and fix.

    After both pass, confirm that all 5 new story files exist:
    - design-system/stories/TenantSwitcher.stories.tsx (from plan 04)
    - design-system/stories/ActiveTenantIndicator.stories.tsx (from plan 04)
    - design-system/stories/BoardSettingsTabs.stories.tsx (from plan 04)
    - design-system/stories/ContentStates.stories.tsx (from plan 05 task 1)
    - design-system/stories/InviteStateRow.stories.tsx (from plan 05 task 2)
  </action>
  <verify>
    <automated>cd /Users/shannendorothee/Projects/evecosys && make check && npm run build-storybook && ls design-system/stories/*.stories.tsx | wc -l</automated>
  </verify>
  <done>
    make check exits 0. npm run build-storybook exits 0. ls design-system/stories/*.stories.tsx shows 13 files (8 original + 5 new). STRB-01 is complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Story args | Pure static mock data; no real credentials or tenant data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Information Disclosure | Story files | accept | Stories use fictional names/emails (charlie@example.com etc.); no real PII |
| T-05-SC | Tampering | npm installs | accept | no new packages; @storybook/test already installed |
</threat_model>

<verification>
cd /Users/shannendorothee/Projects/evecosys && make check && npm run build-storybook
</verification>

<success_criteria>
- design-system/stories/ContentStates.stories.tsx exports: Loading, Empty, EmptyWithCta, Error, Restricted, Unavailable
- design-system/stories/InviteStateRow.stories.tsx exports: Sent, Expiring, Accepted, Expired, Revoked, LimitedAccess
- InviteStateRow LimitedAccess story: isLimitedAccess=true in args
- make check exits 0 (all 5 CI jobs: lint, test, tokens, build, audit)
- npm run build-storybook exits 0
- Total story files in design-system/stories/: 13 (8 original + 5 new)
- STRB-01 requirement fully satisfied: TenantSwitcher, ActiveTenantIndicator, BoardSettingsTabs, ContentStates, InviteStateRow all have Storybook stories covering all key states
</success_criteria>

<output>
Create `.planning/phases/05-storybook-coverage/05-05-SUMMARY.md` when done
</output>
