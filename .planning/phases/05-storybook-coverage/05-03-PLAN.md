---
phase: 05-storybook-coverage
plan: 03
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - app/(dashboard)/board/layout.tsx
  - app/(dashboard)/driver/layout.tsx
  - app/(dashboard)/manager/layout.tsx
  - components/layout/PlatformShell.tsx
  - app/(dashboard)/board/error.tsx
  - app/(dashboard)/manager/error.tsx
  - app/(dashboard)/driver/error.tsx
  - app/(dashboard)/board/settings/users/page.tsx
  - components/layout/DashboardShell.tsx
  - design-system/stories/DashboardShell.stories.tsx
autonomous: false
requirements:
  - STRB-01

must_haves:
  truths:
    - "All three role layouts (board, driver, manager) import LeftRailShell, not DashboardShell"
    - "PlatformShell uses LeftRailShell and preserves TenantProvider wrapping and ActiveTenantIndicator"
    - "All three per-role error.tsx files render ErrorState from ContentStates"
    - "DashboardShell.tsx is deleted or marked deprecated in the same commit as layout migration"
    - "DashboardShell.stories.tsx is updated to reflect LeftRailShell (no broken import)"
    - "Board users page renders InviteStateRow components (prop-driven mock data)"
    - "make build exits 0 (no broken imports)"
  artifacts:
    - path: "app/(dashboard)/board/layout.tsx"
      provides: "Board layout using LeftRailShell with Lucide icon node NAV array"
    - path: "app/(dashboard)/driver/layout.tsx"
      provides: "Driver layout using LeftRailShell with Lucide icon node NAV array"
    - path: "app/(dashboard)/manager/layout.tsx"
      provides: "Manager layout using LeftRailShell with Lucide icon node NAV array"
    - path: "components/layout/PlatformShell.tsx"
      provides: "Platform admin shell using LeftRailShell; preserves TenantProvider + ActiveTenantIndicator"
  key_links:
    - from: "app/(dashboard)/board/layout.tsx"
      to: "components/layout/LeftRailShell.tsx"
      via: "import LeftRailShell and pass navItems as React node icons"
    - from: "components/layout/PlatformShell.tsx"
      to: "components/platform/TenantContext.tsx"
      via: "TenantProvider wrapper preserved around LeftRailShell"
---

<objective>
Rewire all four layout consumers (board, driver, manager, PlatformShell) from the old DashboardShell to the new LeftRailShell. Update error boundary files to use ErrorState. Wire InviteStateRow into the board users page. Retire DashboardShell.tsx and update its story in the same commit to prevent broken Storybook builds.

Purpose: Completes the shell migration so the live app uses the new light left-rail layout for all roles.
Output: 4 migrated layouts + 3 updated error files + users page update + DashboardShell retired + story updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-storybook-coverage/05-CONTEXT.md
@.planning/phases/05-storybook-coverage/05-RESEARCH.md
@.planning/phases/05-storybook-coverage/05-01-SUMMARY.md
@.planning/phases/05-storybook-coverage/05-02-SUMMARY.md
@app/(dashboard)/board/layout.tsx
@app/(dashboard)/driver/layout.tsx
@app/(dashboard)/manager/layout.tsx
@components/layout/PlatformShell.tsx
@components/layout/DashboardShell.tsx
@design-system/stories/DashboardShell.stories.tsx
@app/(dashboard)/board/settings/users/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all role layouts + retire DashboardShell + update story</name>
  <files>
    app/(dashboard)/board/layout.tsx,
    app/(dashboard)/driver/layout.tsx,
    app/(dashboard)/manager/layout.tsx,
    components/layout/PlatformShell.tsx,
    components/layout/DashboardShell.tsx,
    design-system/stories/DashboardShell.stories.tsx
  </files>
  <read_first>
    1. app/(dashboard)/board/layout.tsx — full file; note current NAV array and DashboardShell usage
    2. app/(dashboard)/driver/layout.tsx — full file; note current NAV array
    3. app/(dashboard)/manager/layout.tsx — full file; note current NAV array
    4. components/layout/PlatformShell.tsx — full file; note TenantProvider and ActiveTenantIndicator usage
    5. components/layout/DashboardShell.tsx — lines 1-50; note the NAV_ICONS map to understand string-to-icon mapping
    6. design-system/stories/DashboardShell.stories.tsx — full file; understand what must be updated
    7. components/layout/LeftRailShell.tsx — from 05-01-SUMMARY.md; understand exact prop interface (navItems shape)
  </read_first>
  <action>
    In each role layout file, replace the DashboardShell import with LeftRailShell from @/components/layout/LeftRailShell.

    Update each NAV array: change from string icon names to Lucide React component instances (React nodes). The current string-to-icon mapping is in DashboardShell's NAV_ICONS map. For each layout:
    - board: uses 'layout-dashboard' → LayoutDashboard, 'settings' → Settings, and others from the file
    - driver: check current NAV array for its icon strings
    - manager: check current NAV array for its icon strings
    Import the required Lucide icons at the top of each layout file. Pass icon as `icon={<LayoutDashboard size={20} />}` (React node, not a string).

    Replace `<DashboardShell navItems={NAV} user={profile as AppUser} alertBell={<AlertBellWrapper role="..." />}>` with `<LeftRailShell navItems={NAV} user={profile as AppUser} alertBell={<AlertBellWrapper role="..." />}>`.

    PlatformShell migration: Read the full file first. Preserve the `<TenantProvider initialName={activeTenantName}>` wrapper AROUND the LeftRailShell. Preserve `<ActiveTenantIndicator />` placement — it should appear in the ContentUtilityBar slot or as part of the alertBell prop. Check the 05-01-SUMMARY.md for how ContentUtilityBar accepts the alertBell slot.

    DashboardShell.tsx: After all consumers are migrated, delete the file contents and replace with a re-export shim that throws a helpful error if accidentally imported, OR delete the file entirely. Deleting the file is cleaner — do this in the same commit as the layout migrations.

    DashboardShell.stories.tsx: Update this file in the SAME commit as deleting DashboardShell.tsx. Rename the story to reflect LeftRailShell. Change meta title to "Compositions/LeftRailShell". Remove any import that references DashboardShell. Mock the new LeftRailShell static structure (same approach as current story — no live hooks, static nav items as React nodes). The existing import of Badge, Button, StatCard, Card from @evecosys/design-system is fine to keep for the story body.

    CRITICAL: DashboardShell.tsx deletion and DashboardShell.stories.tsx update must happen in the same commit. If they are separated, `npm run build-storybook` will crash between commits.
  </action>
  <verify>
    <automated>cd /Users/shannendorothee/Projects/evecosys && make typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>
    All three role layout.tsx files import LeftRailShell, not DashboardShell. PlatformShell.tsx imports LeftRailShell and preserves TenantProvider. components/layout/DashboardShell.tsx does not exist (deleted). design-system/stories/DashboardShell.stories.tsx references LeftRailShell (not DashboardShell). make typecheck exits 0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All role layouts (board, driver, manager) and PlatformShell migrated to LeftRailShell. The light left-rail shell with BrandHeader, SidebarSearch, nav items, and AccountBlock should now render for all roles. DashboardShell.tsx has been deleted and its story updated.
  </what-built>
  <how-to-verify>
    1. Run `make dev` to start the dev server at http://localhost:3000
    2. Log in as a board member — navigate to /board. Confirm: left rail visible (not dark topbar), nav items show icon nodes (not blank), AccountBlock shows user avatar/name
    3. Navigate to /manager — same light left-rail layout
    4. Navigate to /platform (as platform admin if available) — left rail visible; ActiveTenantIndicator visible in content utility bar area
    5. Confirm no console errors in browser DevTools
    6. Run `npm run build-storybook` — must exit 0 (no broken DashboardShell import)
  </how-to-verify>
  <resume-signal>Type "approved" when shell renders correctly in all roles, or describe what is broken</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Wire ErrorState to error.tsx files + InviteStateRow to users page</name>
  <files>
    app/(dashboard)/board/error.tsx,
    app/(dashboard)/manager/error.tsx,
    app/(dashboard)/driver/error.tsx,
    app/(dashboard)/board/settings/users/page.tsx
  </files>
  <read_first>
    1. app/(dashboard)/board/error.tsx — full file; note current error UI implementation
    2. app/(dashboard)/manager/error.tsx — full file
    3. app/(dashboard)/driver/error.tsx — full file
    4. app/(dashboard)/board/settings/users/page.tsx — full file; note InviteForm + MemberTable composition
    5. components/layout/shell/ContentStates.tsx — from 05-02-SUMMARY.md; understand ErrorState props interface
    6. components/board/settings/InviteStateRow.tsx — from 05-02-SUMMARY.md; understand props interface
  </read_first>
  <action>
    Update all three error.tsx files to render ErrorState from @/components/layout/shell/ContentStates. Each error.tsx is a Next.js error boundary — must be a 'use client' component with `error` and `reset` props. Wire reset to ErrorState's onRetry prop. The "Report issue" button can be a no-op in this phase.

    Update app/(dashboard)/board/settings/users/page.tsx to render InviteStateRow components below the existing InviteForm + MemberTable. Since InviteStateRow has no live data source (per RESEARCH.md Risk 7 and D-05 scope), render a small section with MOCK placeholder invite rows demonstrating all 5 lifecycle states. Add a clear comment: "// TODO: replace with real invite records from DB query when invitations table is added". Import InviteStateRow from @/components/board/settings/InviteStateRow. Use static mock data matching the InviteStateRowProps shape.

    Do NOT add a DB migration for an invitations table — this is out of scope per D-05 research note.
  </action>
  <verify>
    <automated>cd /Users/shannendorothee/Projects/evecosys && make typecheck && make lint 2>&1 | tail -10</automated>
  </verify>
  <done>
    All three error.tsx files render ErrorState and pass reset to onRetry. app/(dashboard)/board/settings/users/page.tsx imports and renders InviteStateRow with mock data. All files have TODO comment about future DB wiring. make typecheck exits 0. make lint exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Layout auth → LeftRailShell | Layouts are RSC; auth check happens before rendering LeftRailShell; no auth bypass risk |
| Error boundary | error.tsx receives error object from Next.js; rendered client-side; no secrets exposed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | error.tsx | mitigate | ErrorState must NOT render raw error.message to users — show generic "Something went wrong" copy only |
| T-05-04 | Spoofing | PlatformShell TenantProvider | accept | TenantProvider initialName comes from auth-checked RSC — no user can inject a false tenant name |
| T-05-SC | Tampering | npm installs | accept | no new packages in this plan |
</threat_model>

<verification>
cd /Users/shannendorothee/Projects/evecosys && make check && npm run build-storybook
</verification>

<success_criteria>
- app/(dashboard)/board/layout.tsx — imports LeftRailShell; NAV uses React node icons
- app/(dashboard)/driver/layout.tsx — imports LeftRailShell; NAV uses React node icons
- app/(dashboard)/manager/layout.tsx — imports LeftRailShell; NAV uses React node icons
- components/layout/PlatformShell.tsx — imports LeftRailShell; TenantProvider still wraps LeftRailShell; ActiveTenantIndicator in content area
- components/layout/DashboardShell.tsx — DELETED
- design-system/stories/DashboardShell.stories.tsx — title changed to "Compositions/LeftRailShell"; no DashboardShell import
- All three per-role error.tsx files render ErrorState
- app/(dashboard)/board/settings/users/page.tsx renders InviteStateRow with mock data and TODO comment
- Human verify: shell renders correctly in browser for all roles (approved by user)
- npm run build-storybook exits 0
- make check exits 0 (all 5 CI jobs green)
</success_criteria>

<output>
Create `.planning/phases/05-storybook-coverage/05-03-SUMMARY.md` when done
</output>
