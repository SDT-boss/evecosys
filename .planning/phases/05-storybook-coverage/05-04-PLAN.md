---
phase: 05-storybook-coverage
plan: 04
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
  - "05-03"
files_modified:
  - components/platform/TenantList.tsx
  - design-system/stories/TenantSwitcher.stories.tsx
  - design-system/stories/ActiveTenantIndicator.stories.tsx
  - design-system/stories/BoardSettingsTabs.stories.tsx
  - design-system/stories/BlockedScreen.stories.tsx
autonomous: true
requirements:
  - STRB-01

must_haves:
  truths:
    - "TenantList.tsx exports an optional onSwitch prop that defaults to the existing setActiveTenant Server Action"
    - "TenantSwitcher.stories.tsx has stories: DefaultList, ActiveRowHighlighted, SwitchingInProgress, SwitchFailure, EmptyTenants"
    - "ActiveTenantIndicator.stories.tsx has stories: Default, NoTenant, Loading"
    - "BoardSettingsTabs.stories.tsx has stories: BrandingTabActive, UsersTabActive, BYODBTabActive, FeatureTogglesTabActive"
    - "BlockedScreen.stories.tsx has story: BlockedState (renders BlockedScreen with no props — it is a zero-prop component)"
    - "All stories render without console errors"
    - "npm run build-storybook exits 0"
  artifacts:
    - path: "components/platform/TenantList.tsx"
      provides: "TenantList with optional onSwitch prop; existing callers unaffected (default to Server Action)"
      exports: ["TenantList", "TenantListProps"]
    - path: "design-system/stories/TenantSwitcher.stories.tsx"
      provides: "5 TenantSwitcher stories"
      exports: ["DefaultList", "ActiveRowHighlighted", "SwitchingInProgress", "SwitchFailure", "EmptyTenants"]
    - path: "design-system/stories/ActiveTenantIndicator.stories.tsx"
      provides: "3 ActiveTenantIndicator stories"
      exports: ["Default", "NoTenant", "Loading"]
    - path: "design-system/stories/BoardSettingsTabs.stories.tsx"
      provides: "4 SettingsTabNav stories"
      exports: ["BrandingTabActive", "UsersTabActive", "BYODBTabActive", "FeatureTogglesTabActive"]
    - path: "design-system/stories/BlockedScreen.stories.tsx"
      provides: "1 BlockedScreen story covering SWIT-04 blocked state"
      exports: ["BlockedState"]
  key_links:
    - from: "design-system/stories/ActiveTenantIndicator.stories.tsx"
      to: "components/platform/TenantContext.tsx"
      via: "TenantProvider decorator wraps each story"
    - from: "design-system/stories/BoardSettingsTabs.stories.tsx"
      to: "components/board/settings/SettingsTabNav.tsx"
      via: "parameters.nextjs.navigation.pathname mocks usePathname"
    - from: "design-system/stories/TenantSwitcher.stories.tsx"
      to: "components/platform/TenantList.tsx"
      via: "onSwitch={fn()} prop (added in Task 0) replaces direct Server Action"
---

<objective>
Refactor TenantList to accept an optional onSwitch prop (making it story-safe), then write Storybook stories for TenantSwitcher, ActiveTenantIndicator, BoardSettingsTabs, and BlockedScreen. These are the D-06 components and SWIT-04 that fulfill STRB-01.

Purpose: Makes all EVE-145 components and SWIT-04 reviewable in isolation without a running app.
Output: TenantList prop refactor + 4 story files covering 13 total story variants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-storybook-coverage/05-CONTEXT.md
@.planning/phases/05-storybook-coverage/05-01-SUMMARY.md
@.planning/phases/05-storybook-coverage/05-02-SUMMARY.md
@.planning/phases/05-storybook-coverage/05-03-SUMMARY.md
@design-system/stories/Badge.stories.tsx
@components/platform/TenantContext.tsx
@components/platform/TenantList.tsx
@components/platform/BlockedScreen.tsx
@components/platform/ActiveTenantIndicator.tsx
@components/board/settings/SettingsTabNav.tsx
</context>

<interfaces>
<!-- Key patterns extracted from existing stories and source components. -->
<!-- Executor uses these directly — no codebase exploration needed for patterns. -->

Existing story pattern (from Badge.stories.tsx):
```typescript
import type { Meta, StoryObj } from "@storybook/react";
const meta: Meta<typeof ComponentName> = {
  title: "Category/ComponentName",
  component: ComponentName,
};
export default meta;
type Story = StoryObj<typeof ComponentName>;
export const StoryName: Story = { args: { ... } };
```

@storybook/nextjs navigation mock (for usePathname):
```typescript
parameters: {
  nextjs: {
    navigation: { pathname: '/board/settings/users' }
  }
}
```

TenantProvider decorator pattern (for components using useTenantContext):
```typescript
import { TenantProvider } from "@/components/platform/TenantContext";
decorators: [
  (Story) => (
    <TenantProvider initialName="Acme Fleet">
      <Story />
    </TenantProvider>
  )
]
```

@storybook/test fn() for Server Action mocks:
```typescript
import { fn } from "@storybook/test";
```

BlockedScreen (components/platform/BlockedScreen.tsx) is a zero-prop component — it accepts no props. Render it as <BlockedScreen /> with no args.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 0: Add optional onSwitch prop to TenantList (story-safe refactor)</name>
  <files>
    components/platform/TenantList.tsx
  </files>
  <read_first>
    1. components/platform/TenantList.tsx — full file; understand current prop interface and how setActiveTenant Server Action is called internally
  </read_first>
  <action>
    Read components/platform/TenantList.tsx in full. Identify where `setActiveTenant` is called (the Server Action for tenant switching). Add an optional prop to the component's props interface:

    `onSwitch?: (id: string) => Promise<{ ok: boolean; error?: string }>`

    Inside the component, wherever `setActiveTenant` is currently called, replace with:
    `(onSwitch ?? setActiveTenant)(id)`

    This means:
    - When `onSwitch` is provided (story context), it calls the provided mock function
    - When `onSwitch` is omitted (all existing layout and page callers), it falls back to the existing Server Action — preserving current behavior exactly

    Do NOT change any other file. No existing consumer of TenantList needs to be updated because the prop is optional and defaults to the existing behavior.

    After editing, run make typecheck to confirm no type errors were introduced.
  </action>
  <verify>
    <automated>cd /Users/shannendorothee/Projects/evecosys && make typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>
    grep -q "onSwitch" components/platform/TenantList.tsx — exits 0. make typecheck exits 0. No existing consumer files (layout files, pages) were modified.
  </done>
</task>

<task type="auto">
  <name>Task 1: TenantSwitcher stories (sidebar dropdown + TenantList states + BlockedScreen)</name>
  <files>
    design-system/stories/TenantSwitcher.stories.tsx,
    design-system/stories/BlockedScreen.stories.tsx
  </files>
  <read_first>
    1. components/layout/shell/TenantSwitcher.tsx — from 05-01-SUMMARY.md; understand exact prop interface
    2. components/platform/TenantList.tsx — understand updated prop interface including onSwitch? (added in Task 0)
    3. components/platform/TenantContext.tsx — understand TenantProvider and useTenantContext shapes
    4. components/platform/BlockedScreen.tsx — full file; confirm this is a zero-prop component (no tenantName or other props)
    5. design-system/stories/Badge.stories.tsx — lines 1-25; understand Meta + StoryObj pattern to follow
    6. DesignSync get_file — projectId "35effb2c-c297-426f-b553-ac4ed7e8cee6", path "screens/shell/shell-kit.tsx" — confirm TenantSwitcher visual spec
  </read_first>
  <action>
    Create design-system/stories/TenantSwitcher.stories.tsx.

    Meta: title "Compositions/TenantSwitcher", component TenantSwitcher (the new sidebar dropdown from components/layout/shell/TenantSwitcher). Include TenantProvider decorator at the meta level since all stories need tenant context.

    Stories targeting D-06 (collapsed state, expanded list, active item, open dropdown) plus SWIT-01/02/03 states (loading, success, failure) which map to TenantList component behavior:

    Section A — sidebar TenantSwitcher component stories (from components/layout/shell/TenantSwitcher):
    1. DefaultList: tenants = [{id:"1",name:"Acme Fleet"},{id:"2",name:"BrightGrid"},{id:"3",name:"SunPath"}], no active selection; TenantProvider initialName="Acme Fleet"
    2. ActiveRowHighlighted: same tenants, activeTenantId set to "1"; onSelect={fn()}

    Section B — TenantList stories for the switching state machine (SWIT-01/02/03). Add these as additional stories in the same file using a wrapper component that renders TenantList. Pass onSwitch={fn()} to avoid the Server Action import (TenantList now accepts onSwitch per Task 0):
    3. SwitchingInProgress: TenantList with pendingTenantId row in loading state — use a Wrapper component that renders TenantList with onSwitch={fn()} and shows spinner row state
    4. SwitchFailure: TenantList with switchError prop set to "Permission denied — contact your admin" and onSwitch={fn()}
    5. EmptyTenants: TenantList with tenants=[] and onSwitch={fn()} showing empty state

    Import TenantList from @/components/platform/TenantList. Import TenantSwitcher from @/components/layout/shell/TenantSwitcher. Import TenantProvider from @/components/platform/TenantContext. Import fn from @storybook/test.

    Use story-level decorators for TenantProvider where needed. Do NOT add TenantProvider to global preview.ts.

    ---

    Create design-system/stories/BlockedScreen.stories.tsx (covers SWIT-04 / ROADMAP SC-1 blocked state).

    Meta: title "Components/BlockedScreen", component BlockedScreen. Import BlockedScreen from @/components/platform/BlockedScreen.

    BlockedScreen is a zero-prop component — it accepts no props and renders its own internal copy. Story:
    6. BlockedState: no args needed — renders <BlockedScreen /> as-is. Use render: () => <BlockedScreen /> if args inference causes issues with a zero-prop component.
  </action>
  <verify>
    <automated>cd /Users/shannendorothee/Projects/evecosys && npm run build-storybook 2>&1 | tail -10</automated>
  </verify>
  <done>
    design-system/stories/TenantSwitcher.stories.tsx exists and exports DefaultList, ActiveRowHighlighted, SwitchingInProgress, SwitchFailure, EmptyTenants stories (5 total). design-system/stories/BlockedScreen.stories.tsx exists and exports BlockedState story (zero-prop render of BlockedScreen). npm run build-storybook exits 0. No console errors in storybook output.
  </done>
</task>

<task type="auto">
  <name>Task 2: ActiveTenantIndicator stories + BoardSettingsTabs stories</name>
  <files>
    design-system/stories/ActiveTenantIndicator.stories.tsx,
    design-system/stories/BoardSettingsTabs.stories.tsx
  </files>
  <read_first>
    1. components/platform/ActiveTenantIndicator.tsx — full file; understand exactly how it reads from useTenantContext
    2. components/board/settings/SettingsTabNav.tsx — full file; understand usePathname usage and TABS array
    3. components/platform/TenantContext.tsx — understand TenantProvider props (initialName shape)
    4. design-system/stories/Badge.stories.tsx — lines 1-30; story pattern reference
  </read_first>
  <action>
    ActiveTenantIndicator.stories.tsx:
    Meta: title "Components/ActiveTenantIndicator", component ActiveTenantIndicator. Import ActiveTenantIndicator from @/components/platform/ActiveTenantIndicator. Import TenantProvider from @/components/platform/TenantContext.

    Each story needs its own TenantProvider decorator with different initialName:
    1. Default: decorator wraps with TenantProvider initialName="Acme Fleet" — shows tenant name and Building2 icon
    2. NoTenant: decorator wraps with TenantProvider initialName={null} — shows "No workspace selected" fallback text
    3. Loading: visually identical to NoTenant (no explicit loading prop on the component per RESEARCH.md) — document in story description that this represents the pre-load state; decorator uses TenantProvider initialName={null}

    Note: TenantProvider decorator must be per-story, not meta-level, since each story needs a different initialName value.

    BoardSettingsTabs.stories.tsx:
    Meta: title "Compositions/BoardSettingsTabs", component SettingsTabNav. Import SettingsTabNav from @/components/board/settings/SettingsTabNav.

    Four stories, each with a different pathname parameter:
    1. BrandingTabActive: parameters: { nextjs: { navigation: { pathname: '/board/settings/branding' } } }
    2. UsersTabActive: parameters: { nextjs: { navigation: { pathname: '/board/settings/users' } } }
    3. BYODBTabActive: parameters: { nextjs: { navigation: { pathname: '/board/settings/byodb' } } }
    4. FeatureTogglesTabActive: parameters: { nextjs: { navigation: { pathname: '/board/settings/toggles' } } }

    Per Risk 2 in RESEARCH.md: the pathname parameter MUST be set per story (not just in meta) to ensure each tab story correctly shows that tab as active.

    SettingsTabNav uses Radix Tabs with asChild Links. No additional mocking needed beyond the pathname — @storybook/nextjs handles next/navigation and next/link automatically.
  </action>
  <verify>
    <automated>cd /Users/shannendorothee/Projects/evecosys && npm run build-storybook 2>&1 | tail -10</automated>
  </verify>
  <done>
    design-system/stories/ActiveTenantIndicator.stories.tsx exports Default, NoTenant, Loading stories. design-system/stories/BoardSettingsTabs.stories.tsx exports BrandingTabActive, UsersTabActive, BYODBTabActive, FeatureTogglesTabActive stories. npm run build-storybook exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Story mock data | Stories use static mock data only; no real tenant data exposed |
| TenantList onSwitch prop | onSwitch is optional; when absent the existing Server Action fires; no new trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Information Disclosure | Story files | accept | Stories are build-time static assets; no credentials or PII in story args |
| T-05-SC | Tampering | npm installs | accept | no new packages; @storybook/test already in devDependencies |
</threat_model>

<verification>
cd /Users/shannendorothee/Projects/evecosys && npm run build-storybook && make tokens
</verification>

<success_criteria>
- components/platform/TenantList.tsx has optional onSwitch? prop; make typecheck exits 0; no existing consumer files changed
- design-system/stories/TenantSwitcher.stories.tsx exports exactly: DefaultList, ActiveRowHighlighted, SwitchingInProgress, SwitchFailure, EmptyTenants
- design-system/stories/BlockedScreen.stories.tsx exports exactly: BlockedState (zero-prop render of BlockedScreen — no tenantName prop)
- design-system/stories/ActiveTenantIndicator.stories.tsx exports exactly: Default, NoTenant, Loading; each story has its own TenantProvider decorator with correct initialName
- design-system/stories/BoardSettingsTabs.stories.tsx exports exactly: BrandingTabActive, UsersTabActive, BYODBTabActive, FeatureTogglesTabActive; each story has parameters.nextjs.navigation.pathname set per-story
- npm run build-storybook exits 0 (clean build, no missing imports, no console errors in build output)
- make tokens exits 0
- All story files use @/components/* import paths, not relative paths
- No hardcoded hex values for colors that have --ds-* tokens
</success_criteria>

<output>
Create `.planning/phases/05-storybook-coverage/05-04-SUMMARY.md` when done
</output>
