---
phase: 02-byodb-registration-service
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - test/unit/lib/tenant/registrationService.test.ts
  - test/unit/lib/tenant/credentials.test.ts
autonomous: true
requirements: [TEST-02]
must_haves:
  truths:
    - "A test proves the happy-path: valid probe → vault store → tenant becomes Active"
    - "A test proves connectivity failure rejects before any vault write"
    - "A test proves rollback: vault.delete is called when the post-store step fails"
    - "Credential parsing is tested for both connection-string and structured inputs"
    - "The full vitest run passes with zero failures"
  artifacts:
    - path: "test/unit/lib/tenant/registrationService.test.ts"
      provides: "Happy-path, connectivity-failure, and rollback tests for BYODBRegistrationService"
      contains: "BYODBRegistrationService"
    - path: "test/unit/lib/tenant/credentials.test.ts"
      provides: "normalizeCredential parsing tests for both input kinds and both engines"
      contains: "normalizeCredential"
  key_links:
    - from: "test/unit/lib/tenant/registrationService.test.ts"
      to: "lib/tenant/registrationService.ts"
      via: "imports BYODBRegistrationService and injects fake ConnectivityProbe + VaultStore"
      pattern: "new BYODBRegistrationService"
    - from: "test/unit/lib/tenant/registrationService.test.ts"
      to: "vault.delete spy"
      via: "asserts delete called on rollback, not called on happy path"
      pattern: "delete.*toHaveBeenCalled"
---

<objective>
Write the TEST-02 unit suite that proves BYODBRegistrationService behaves correctly: happy-path registration, connectivity-failure rejection, and rollback-on-failure — using injected fakes for the ConnectivityProbe and VaultStore (no live DB). Also test the credential parser for both input forms and both engines.

Purpose: Satisfy TEST-02 and lock in the phase's observable behaviors. The fakes exercise the orchestration's control flow (probe-before-store, transition-on-success, delete-on-failure) deterministically.
Output: registrationService.test.ts and credentials.test.ts, all passing under `npm test`.
</objective>

<execution_context>
@/Users/shannendorothee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shannendorothee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md

@lib/tenant/types.ts
@test/unit/lib/tenant/stateMachine.test.ts

<interfaces>
<!-- Code under test, from Plans 01 + 02. Import and inject fakes against these. -->

From lib/tenant/registrationService.ts (02-02):
```typescript
export interface RegistrationResult { tenant: Tenant; secretId: string }
export class BYODBRegistrationService {
  constructor(probe: ConnectivityProbe, vault: VaultStore)
  register(tenant: Tenant, input: BYODBCredentialInput): Promise<RegistrationResult>
}
```
Behavior: throws ConnectivityError if tenant not in 'Provisioning'; calls normalizeCredential (throws CredentialValidationError on bad input) before probing; probe returning {reachable:false} or {ownsSchema:false} → ConnectivityError, vault.store NOT called; on store success then a failing transition → vault.delete(secretId) called once, error re-thrown; happy path returns {tenant:{...,state:'Active'}, secretId}.

From lib/tenant/probe.ts: `interface ConnectivityProbe { probe(params): Promise<ProbeResult> }`, `interface ProbeResult { reachable; ownsSchema; error? }`, `class ConnectivityError`.
From lib/tenant/vault.ts: `interface VaultStore { store(name,secret): Promise<{secretId}>; delete(secretId): Promise<void> }`.
From lib/tenant/credentials.ts: `normalizeCredential(input)`, `BYODBCredentialInput`, `CredentialValidationError`, `BYODBEngine`.
From lib/tenant/types.ts: `Tenant { id, state: TenantState, created_at, updated_at }`.

Test conventions (from test/unit/lib/tenant/stateMachine.test.ts): vitest globals (describe/it/expect, no imports needed for them), `@/` path alias, `vi.fn()` for spies, a local `tenant(state)` helper factory. Test file lives under test/unit/lib/tenant/.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Credential parser unit tests</name>
  <files>test/unit/lib/tenant/credentials.test.ts</files>
  <read_first>
    - lib/tenant/credentials.ts (exact exports + parsing rules + which fields throw CredentialValidationError)
    - test/unit/lib/tenant/stateMachine.test.ts (test style: vitest globals, it.each, @/ imports)
  </read_first>
  <action>
    Create `test/unit/lib/tenant/credentials.test.ts` importing `{ normalizeCredential, CredentialValidationError, type BYODBCredentialInput } from '@/lib/tenant/credentials'`. Add a `describe('normalizeCredential', ...)` with these tests:
    1. Structured postgres input `{ kind:'structured', params:{ engine:'postgres', host:'db.example.com', port:5432, database:'app', user:'u', password:'p', ssl:true } }` → returns params with `engine:'postgres'`, `host:'db.example.com'`, `port:5432`.
    2. Structured mysql input with `engine:'mysql', port:3306` → returns `engine:'mysql'`, `port:3306`.
    3. Connection-string postgres `{ kind:'connectionString', engine:'postgres', connectionString:'postgresql://user:secret@host.example:6543/mydb' }` → returns `host:'host.example'`, `port:6543`, `database:'mydb'`, `user:'user'`, `password:'secret'`, `engine:'postgres'`.
    4. Connection-string postgres with no port → `port` defaults to `5432`.
    5. Connection-string mysql `'mysql://u:p@h:3307/d'` with `engine:'mysql'` → `port:3307`, `engine:'mysql'`.
    6. Engine mismatch: `{ kind:'connectionString', engine:'mysql', connectionString:'postgresql://u:p@h/d' }` → `expect(() => normalizeCredential(input)).toThrow(CredentialValidationError)`.
    7. Structured input missing password (`password:''`) → throws `CredentialValidationError`.
    8. The thrown error message must NOT contain the password value: build a structured input with `password:'sup3rsecret'` but an invalid port (e.g. `port:0`), catch the error, `expect((e as Error).message).not.toContain('sup3rsecret')`.

    Use the exact literal values above so assertions are concrete.
  </action>
  <verify>
    <automated>npx vitest run test/unit/lib/tenant/credentials.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "normalizeCredential" test/unit/lib/tenant/credentials.test.ts`
    - `grep -q "connectionString" test/unit/lib/tenant/credentials.test.ts && grep -q "kind: *'structured'\|kind:'structured'" test/unit/lib/tenant/credentials.test.ts`
    - `grep -q "CredentialValidationError" test/unit/lib/tenant/credentials.test.ts`
    - `grep -q "not.toContain" test/unit/lib/tenant/credentials.test.ts` (password-leak assertion present)
    - `npx vitest run test/unit/lib/tenant/credentials.test.ts` exits 0 with all tests passing
  </acceptance_criteria>
  <done>credentials.test.ts covers both input kinds, both engines, engine-mismatch rejection, missing-field rejection, and password-not-leaked-in-error; all tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Registration service tests — happy path, connectivity failure, rollback</name>
  <files>test/unit/lib/tenant/registrationService.test.ts</files>
  <read_first>
    - lib/tenant/registrationService.ts (register control flow + RegistrationResult shape + which errors are thrown when)
    - lib/tenant/probe.ts (ConnectivityProbe interface + ConnectivityError + ProbeResult)
    - lib/tenant/vault.ts (VaultStore interface)
    - lib/tenant/credentials.ts (BYODBCredentialInput shape for building a valid input)
    - test/unit/api/users-create.test.ts (vi.fn() spy + mock-injection pattern used in this repo)
    - test/unit/lib/tenant/stateMachine.test.ts (tenant(state) helper factory pattern)
  </read_first>
  <action>
    Create `test/unit/lib/tenant/registrationService.test.ts`.

    Imports: `{ BYODBRegistrationService } from '@/lib/tenant/registrationService'`, `{ ConnectivityError, type ConnectivityProbe, type ProbeResult } from '@/lib/tenant/probe'`, `{ type VaultStore } from '@/lib/tenant/vault'`, `{ type BYODBCredentialInput } from '@/lib/tenant/credentials'`, `{ type Tenant, type TenantState } from '@/lib/tenant/types'`.

    Helpers at top:
    - `function tenant(state: TenantState): Tenant { return { id: 't-1', state, created_at: '2026-01-01T00:00:00Z', updated_at: '2026-01-01T00:00:00Z' } }`.
    - `const validInput: BYODBCredentialInput = { kind: 'structured', params: { engine: 'postgres', host: 'h', port: 5432, database: 'd', user: 'u', password: 'p' } }`.
    - `function fakeProbe(result: ProbeResult): ConnectivityProbe { return { probe: vi.fn().mockResolvedValue(result) } }`.
    - `function fakeVault(overrides: Partial<VaultStore> = {}): VaultStore { return { store: vi.fn().mockResolvedValue({ secretId: 'sec-123' }), delete: vi.fn().mockResolvedValue(undefined), ...overrides } }`.

    Tests:
    1. Happy path: `const probe = fakeProbe({ reachable:true, ownsSchema:true })`, `const vault = fakeVault()`, `const svc = new BYODBRegistrationService(probe, vault)`, `const res = await svc.register(tenant('Provisioning'), validInput)`. Assert: `res.tenant.state === 'Active'`; `res.secretId === 'sec-123'`; `vault.store` called exactly once; `vault.delete` NOT called (`expect(vault.delete).not.toHaveBeenCalled()`).
    2. Probe unreachable: `fakeProbe({ reachable:false, ownsSchema:false, error:'connection refused' })`. `await expect(svc.register(tenant('Provisioning'), validInput)).rejects.toBeInstanceOf(ConnectivityError)`; assert `vault.store` NOT called.
    3. Probe reachable but no schema ownership: `fakeProbe({ reachable:true, ownsSchema:false })` → rejects with ConnectivityError; `vault.store` NOT called.
    4. Wrong starting state: `await expect(svc.register(tenant('Registered'), validInput)).rejects.toBeInstanceOf(ConnectivityError)`; probe NOT called (`expect(probe.probe).not.toHaveBeenCalled()`).
    5. Rollback on post-store failure: build a tenant whose transition to Active will fail. Since register hard-codes `transition(tenant.state, 'Active')` and only 'Provisioning'→'Active' is valid, force failure by making `vault.store` succeed but the transition path throw — simulate by passing a tenant in 'Provisioning' and a vault whose `delete` is a spy, then make the post-store step throw via a `store` that returns a secretId but a `transition` that fails. To trigger a deterministic post-store failure without monkeypatching the state machine, use a vault whose `store` resolves `{ secretId:'sec-xyz' }` and whose `delete` is a spy, and inject a probe success, but make the transition fail by registering a tenant whose state is 'Provisioning' (valid) — instead, simulate the post-store failure by having `vault.store` resolve and then asserting rollback via a `store` mock that resolves and a deliberately throwing follow-up: set `vault.store = vi.fn().mockResolvedValue({ secretId:'sec-xyz' })` and spy `delete`, and make `transition` fail by passing an input that is valid but use `vi.spyOn` on the stateMachine module: `import * as sm from '@/lib/tenant/stateMachine'; vi.spyOn(sm,'transition').mockImplementation(()=>{ throw new Error('boom') })`. Then `await expect(svc.register(tenant('Provisioning'), validInput)).rejects.toThrow('boom')` and assert `expect(vault.delete).toHaveBeenCalledWith('sec-xyz')` and `expect(vault.delete).toHaveBeenCalledTimes(1)`. Restore the spy with `vi.restoreAllMocks()` in an `afterEach`.

    Add `afterEach(() => vi.restoreAllMocks())`.
  </action>
  <verify>
    <automated>npx vitest run test/unit/lib/tenant/registrationService.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "new BYODBRegistrationService" test/unit/lib/tenant/registrationService.test.ts`
    - `grep -q "state).toBe('Active')\|state === 'Active'\|toBe('Active')" test/unit/lib/tenant/registrationService.test.ts`
    - `grep -q "rejects.toBeInstanceOf(ConnectivityError)" test/unit/lib/tenant/registrationService.test.ts`
    - `grep -q "store).not.toHaveBeenCalled\|store).not" test/unit/lib/tenant/registrationService.test.ts` (no-store-on-failure assertion)
    - `grep -q "delete).toHaveBeenCalledWith('sec-xyz')\|toHaveBeenCalledWith('sec" test/unit/lib/tenant/registrationService.test.ts` (rollback assertion)
    - `grep -q "delete).not.toHaveBeenCalled" test/unit/lib/tenant/registrationService.test.ts` (no-rollback-on-happy-path assertion)
    - `npx vitest run test/unit/lib/tenant/registrationService.test.ts` exits 0 with all tests passing
  </acceptance_criteria>
  <done>registrationService.test.ts proves happy-path (Active + store once + no delete), connectivity failure (reject + no store) for both unreachable and no-ownership cases, wrong-state rejection (no probe), and rollback (delete called once with the secretId); all tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Full suite green check</name>
  <files>test/unit/lib/tenant/registrationService.test.ts</files>
  <read_first>
    - test/unit/lib/tenant/registrationService.test.ts (the file from Task 2)
    - test/unit/lib/tenant/credentials.test.ts (the file from Task 1)
  </read_first>
  <action>
    Run the entire unit suite and confirm zero failures. If any pre-existing test regressed because of the new lib/tenant code or the package.json change, investigate and fix the root cause in the offending source/test file (do NOT delete or `.skip` tests to make them pass). Re-run until the whole suite is green. This is the gate that proves TEST-02 holds within the full project, not just in isolation.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits with code 0
    - The final vitest summary line reports `0 failed` (e.g. contains `failed` only in a `0 failed` context, or shows all files passed)
    - Both new test files appear in the run output: `npx vitest run 2>&1 | grep -E "credentials.test.ts|registrationService.test.ts"` matches both
  </acceptance_criteria>
  <done>`npx vitest run` passes with zero failures and includes both new tenant test files.</done>
</task>

</tasks>

<verification>
- `npx vitest run` exits 0 with no failures
- registrationService.test.ts asserts: Active on success, store-once + no-delete on happy path, no-store on probe failure, delete-once-with-secretId on rollback, no-probe on wrong state
- credentials.test.ts asserts both input kinds, both engines, mismatch rejection, and password not leaked in error
</verification>

<success_criteria>
- TEST-02 satisfied: happy-path, connectivity-failure, and rollback-on-failure are all covered by passing unit tests
- Tests use injected fakes (no live DB), matching the repo's vi.fn mock-injection convention
- Full project test suite is green
</success_criteria>

<output>
After completion, create `.planning/phases/02-byodb-registration-service/02-03-SUMMARY.md`
</output>
