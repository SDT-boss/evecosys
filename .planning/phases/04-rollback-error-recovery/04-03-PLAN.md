---
phase: 04-rollback-error-recovery
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [TEST-04]
user_setup: []
must_haves:
  truths:
    - "SEC-02, SEC-03, and TEST-03 are marked complete in REQUIREMENTS.md (Phase 3 gap audit close-out)"
    - "The full Vitest unit suite passes with zero failures including the new rollback.test.ts"
    - "REQUIREMENTS.md traceability table shows zero remaining Pending rows except deferred v2 items"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirement statuses for SEC-02/SEC-03/TEST-03 and traceability table"
      contains: "[x] **SEC-02**"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "supabase/migrations/20260609140000_rls_audit_no_delete.sql"
      via: "SEC-02 audited as satisfied"
      pattern: "SEC-02"
    - from: ".planning/REQUIREMENTS.md"
      to: "lib/supabase/service.ts"
      via: "SEC-03 audited as satisfied (server-only)"
      pattern: "SEC-03"
---

<objective>
Close out the Phase 3 documentation gap: REQUIREMENTS.md still lists SEC-02, SEC-03, and TEST-03 as "Pending" even though the Phase 3 VERIFICATION.md (and the Phase 4 RESEARCH.md gap audit) confirm all three are fully implemented and tested. Audit the implementations exist, then mark them complete. Finally, prove the full Vitest unit suite — including the new rollback.test.ts from Plan 04-01 — passes with zero failures (the unit half of TEST-04).

Purpose: Satisfies TEST-04 (100% unit suite compliance) and corrects requirement traceability bookkeeping. No production code changes — this is verification + documentation.
Output: Updated `.planning/REQUIREMENTS.md` with SEC-02/SEC-03/TEST-03 marked complete, and a green full unit suite.
</objective>

<execution_context>
@/Users/shannendorothee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shannendorothee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/04-rollback-error-recovery/04-RESEARCH.md
@.planning/phases/04-rollback-error-recovery/04-CONTEXT.md

<interfaces>
<!-- Gap audit evidence — these files prove SEC-02/SEC-03/TEST-03 are satisfied. -->
SEC-02 (RLS cross-tenant isolation): supabase/migrations/20260609120000_create_tenants.sql (select/insert/update own policies) + supabase/migrations/20260609140000_rls_audit_no_delete.sql
SEC-03 (service-role gating): lib/supabase/service.ts contains `import 'server-only'` and createServiceClient() using SUPABASE_SERVICE_ROLE_KEY
TEST-03 (cross-tenant isolation tests): test/unit/lib/tenant/tenantIsolation.test.ts (TenantAuthGuard tests, all green)
Authoritative prior verification: .planning/phases/03-tenant-isolation-layer/03-VERIFICATION.md (SEC-02/SEC-03/TEST-03 = SATISFIED)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit Phase 3 requirements still exist and are implemented</name>
  <files></files>
  <read_first>
    - .planning/phases/03-tenant-isolation-layer/03-VERIFICATION.md (confirm SEC-02/SEC-03/TEST-03 marked SATISFIED)
    - lib/supabase/service.ts (confirm `import 'server-only'` present for SEC-03)
    - test/unit/lib/tenant/tenantIsolation.test.ts (confirm cross-tenant isolation tests exist for TEST-03)
    - supabase/migrations/20260609140000_rls_audit_no_delete.sql (confirm RLS audit migration exists for SEC-02)
  </read_first>
  <action>
    Run a read-only audit confirming each implementation artifact exists before changing any status. Verify all three of the following resolve true. Do NOT modify any code or migration — this task only gathers evidence:
    1. SEC-03 — `grep -q "import 'server-only'" lib/supabase/service.ts`
    2. TEST-03 — `test/unit/lib/tenant/tenantIsolation.test.ts` exists and `npx vitest run test/unit/lib/tenant/tenantIsolation.test.ts` exits 0
    3. SEC-02 — `supabase/migrations/20260609140000_rls_audit_no_delete.sql` exists AND `supabase/migrations/20260609120000_create_tenants.sql` contains RLS policy definitions (grep for `create policy` / `using (`)
    If any check fails, STOP and report the gap rather than marking the requirement complete (the RESEARCH.md audit predicts all pass — a failure means real implementation work is needed and this plan's assumption is wrong).
  </action>
  <verify>
    <automated>grep -q "import 'server-only'" lib/supabase/service.ts && test -f test/unit/lib/tenant/tenantIsolation.test.ts && test -f supabase/migrations/20260609140000_rls_audit_no_delete.sql && npx vitest run test/unit/lib/tenant/tenantIsolation.test.ts >/dev/null 2>&1 && echo AUDIT_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "import 'server-only'" lib/supabase/service.ts` exits 0 (SEC-03 satisfied)
    - `test -f test/unit/lib/tenant/tenantIsolation.test.ts` exits 0 (TEST-03 artifact exists)
    - `npx vitest run test/unit/lib/tenant/tenantIsolation.test.ts` exits 0 (TEST-03 green)
    - `test -f supabase/migrations/20260609140000_rls_audit_no_delete.sql` exits 0 (SEC-02 satisfied)
    - `grep -qi "create policy" supabase/migrations/20260609120000_create_tenants.sql` exits 0 (SEC-02 RLS policies present)
  </acceptance_criteria>
  <done>All three Phase 3 requirements (SEC-02, SEC-03, TEST-03) confirmed implemented and tested via grep/test-run evidence; safe to mark complete.</done>
</task>

<task type="auto">
  <name>Task 2: Mark SEC-02/SEC-03/TEST-03 complete and run full unit suite</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 28-29 SEC-02/SEC-03; line 42 TEST-03; traceability table lines 79-87)
  </read_first>
  <action>
    In .planning/REQUIREMENTS.md make these exact edits (the audit in Task 1 must have passed first):
    1. Line 28: change `- [ ] **SEC-02**:` to `- [x] **SEC-02**:` (keep the rest of the line identical).
    2. Line 29: change `- [ ] **SEC-03**:` to `- [x] **SEC-03**:`.
    3. Line 42: change `- [ ] **TEST-03**:` to `- [x] **TEST-03**:`.
    4. In the Traceability table: change `| SEC-02 | Phase 3 | Pending |` to `| SEC-02 | Phase 3 | Complete |`; `| SEC-03 | Phase 3 | Pending |` to `| SEC-03 | Phase 3 | Complete |`; `| TEST-03 | Phase 3 | Pending |` to `| TEST-03 | Phase 3 | Complete |`.
    Do NOT change ROLLBACK-01/02/03 or TEST-04 — those are completed by the Phase 4 verify/finalize step, not this plan.

    Then run the full Vitest unit suite to prove the unit half of TEST-04: `npx vitest run`. It MUST exit 0 with zero failures, now including the new test/unit/lib/tenant/rollback.test.ts from Plan 04-01. If any test fails, STOP and report — do not mark anything further.
  </action>
  <verify>
    <automated>grep -q "\[x\] \*\*SEC-02\*\*" .planning/REQUIREMENTS.md && grep -q "\[x\] \*\*SEC-03\*\*" .planning/REQUIREMENTS.md && grep -q "\[x\] \*\*TEST-03\*\*" .planning/REQUIREMENTS.md && npx vitest run 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains `- [x] **SEC-02**:`
    - `.planning/REQUIREMENTS.md` contains `- [x] **SEC-03**:`
    - `.planning/REQUIREMENTS.md` contains `- [x] **TEST-03**:`
    - `grep -c "| Pending |" .planning/REQUIREMENTS.md` returns only rows for ROLLBACK-01/02/03 and TEST-04 (the four Phase 4 requirements still pending until phase finalize) — SEC-02/SEC-03/TEST-03 no longer Pending in the traceability table
    - `npx vitest run` exits 0 with zero failing tests (full unit suite green, includes rollback.test.ts)
  </acceptance_criteria>
  <done>SEC-02/SEC-03/TEST-03 marked complete in both the checklist and traceability table; full unit suite is green including the new rollback tests.</done>
</task>

</tasks>

<verification>
- `grep -q "\[x\] \*\*SEC-02\*\*" .planning/REQUIREMENTS.md` and same for SEC-03, TEST-03 — all marked complete
- `npx vitest run` — full unit suite green (unit half of TEST-04)
- Traceability table no longer shows SEC-02/SEC-03/TEST-03 as Pending
</verification>

<success_criteria>
- Phase 3 gap audit confirmed implementations exist (no code changes needed)
- SEC-02, SEC-03, TEST-03 marked complete in REQUIREMENTS.md checklist and traceability table
- Full Vitest unit suite passes with zero failures, including the new rollback.test.ts (unit half of TEST-04)
</success_criteria>

<output>
After completion, create `.planning/phases/04-rollback-error-recovery/04-03-SUMMARY.md`
</output>
