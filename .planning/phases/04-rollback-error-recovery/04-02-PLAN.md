---
phase: 04-rollback-error-recovery
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - vitest.integration.config.mts
  - test/integration/tenant-provisioning.test.ts
  - Makefile
autonomous: false
requirements: [ROLLBACK-03, TEST-04]
user_setup: []
must_haves:
  truths:
    - "Integration tests run in a Node environment against real local Supabase, loading NEXT_PUBLIC_SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY from .env.local"
    - "The full provisioning happy path stores a credential in Vault and the secret can be read back via service-role client"
    - "A post-store rollback deletes the Vault secret so no orphaned credential remains (ROLLBACK-03 verified against real Vault)"
    - "RLS enforces cross-tenant isolation: an authenticated client for user A receives zero rows querying user B's tenant"
    - "make test-integration runs the integration suite against local Supabase"
  artifacts:
    - path: "vitest.integration.config.mts"
      provides: "Node-environment Vitest config with .env.local loading"
      contains: "environment: 'node'"
    - path: "test/integration/tenant-provisioning.test.ts"
      provides: "Real-Supabase provisioning lifecycle + RLS + Vault tests"
      min_lines: 80
    - path: "Makefile"
      provides: "test-integration target"
      contains: "test-integration"
  key_links:
    - from: "test/integration/tenant-provisioning.test.ts"
      to: "lib/tenant/registrationService.ts"
      via: "import BYODBRegistrationService under test"
      pattern: "import.*BYODBRegistrationService"
    - from: "test/integration/tenant-provisioning.test.ts"
      to: "Supabase Vault RPC (store_byodb_secret/delete_byodb_secret)"
      via: "SupabaseVaultStore against real client"
      pattern: "SupabaseVaultStore"
    - from: "vitest.integration.config.mts"
      to: ".env.local"
      via: "loadEnv"
      pattern: "loadEnv"
---

<objective>
Add Vitest integration tests that exercise the full BYODB provisioning lifecycle against a real local Supabase instance (Docker): happy-path credential storage in Vault, rollback deletion of orphaned secrets (ROLLBACK-03), RLS cross-tenant isolation, and the Vault credential store/delete lifecycle. These run in a separate Node-environment Vitest config so the existing jsdom unit config (293 green tests) is left untouched.

Purpose: Satisfies the integration half of TEST-04 (full Vitest unit suite AND integration tests pass against real local Supabase) and proves ROLLBACK-03 against the real Supabase Vault RPC rather than a mock.
Output: `vitest.integration.config.mts`, `test/integration/tenant-provisioning.test.ts`, and a `make test-integration` target.
</objective>

<execution_context>
@/Users/shannendorothee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shannendorothee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-rollback-error-recovery/04-CONTEXT.md
@.planning/phases/04-rollback-error-recovery/04-RESEARCH.md
@.planning/phases/04-rollback-error-recovery/04-VALIDATION.md

<interfaces>
<!-- Exact contracts the executor needs. Extracted from the codebase. -->

From lib/tenant/registrationService.ts (after Plan 04-01 hardening):
```typescript
export class BYODBRegistrationService {
  constructor(probe: ConnectivityProbe, vault: VaultStore) {}
  async register(tenant: Tenant, input: BYODBCredentialInput): Promise<{ tenant: Tenant; secretId: string }>
}
```

From lib/tenant/vaultStore.ts:
```typescript
// SupabaseVaultStore accepts an optional injected SupabaseClient (per Phase 02 decision)
export class SupabaseVaultStore implements VaultStore { /* store(name, secret) -> {secretId}; delete(secretId) */ }
```

From lib/supabase/service.ts (server-only — do NOT import in integration tests; build a raw client instead):
```typescript
export function createServiceClient() // uses NEXT_PUBLIC_SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY
```
NOTE: lib/supabase/service.ts has `import 'server-only'`. Under environment:'node' with no Next runtime this would throw, so integration tests construct a raw client directly:
`import { createClient } from '@supabase/supabase-js'`.

From lib/tenant/types.ts:
```typescript
// Tenant: { id, owner_id, state: TenantState, created_at, updated_at }
// TenantState includes 'Provisioning', 'Registered', 'Active'
```

Env vars available in .env.local (confirmed in .env.example):
```
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY
```

Relevant migrations (already applied via `make migrate`):
- supabase/migrations/20260609120000_create_tenants.sql  (tenants table + RLS: select/insert/update own via auth.uid() = owner_id)
- supabase/migrations/20260609130000_byodb_vault_rpc.sql  (store_byodb_secret, delete_byodb_secret RPCs)
- supabase/migrations/20260609140000_rls_audit_no_delete.sql
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create vitest.integration.config.mts and make test-integration target</name>
  <files>vitest.integration.config.mts, Makefile</files>
  <read_first>
    - vitest.config.mts (replicate the resolve.alias for '@' and 'server-only'; do NOT modify this file)
    - Makefile (lines 33-45 — the existing test / check targets and the .PHONY list on lines 1-3)
    - .env.example (confirm exact env var names: NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, NEXT_PUBLIC_SUPABASE_ANON_KEY)
  </read_first>
  <action>
    Create vitest.integration.config.mts with exactly this structure (separate config, NOT a workspace edit to vitest.config.mts — per RESEARCH.md recommendation the working unit config must stay untouched):
    ```typescript
    import { defineConfig, loadEnv } from 'vitest/config'
    import path from 'path'

    export default defineConfig(({ mode }) => {
      const env = loadEnv(mode, process.cwd(), '')
      return {
        resolve: {
          alias: {
            '@': path.resolve(__dirname),
            'server-only': path.resolve(__dirname, 'test/__mocks__/server-only.ts'),
          },
        },
        test: {
          name: 'integration',
          environment: 'node',
          globals: true,
          include: ['test/integration/tenant-provisioning.test.ts'],
          exclude: ['node_modules/**', 'e2e/**'],
          env: {
            NEXT_PUBLIC_SUPABASE_URL: env.NEXT_PUBLIC_SUPABASE_URL,
            NEXT_PUBLIC_SUPABASE_ANON_KEY: env.NEXT_PUBLIC_SUPABASE_ANON_KEY,
            SUPABASE_SERVICE_ROLE_KEY: env.SUPABASE_SERVICE_ROLE_KEY,
          },
        },
      }
    })
    ```
    In the Makefile: add `test-integration` to the .PHONY line (line 1-3 block), and add this target immediately after the existing `test-watch` target (after line 37):
    ```makefile
    test-integration: ## Run Vitest integration tests against local Supabase (requires `make db-start`)
    	npx vitest run --config vitest.integration.config.mts
    ```
    Do NOT add it to the `check` target — integration tests require Docker and are local-only verification (per RESEARCH.md Open Question 2: CI `make test` covers unit tests only).
  </action>
  <verify>
    <automated>grep -q "environment: 'node'" vitest.integration.config.mts && grep -q "loadEnv" vitest.integration.config.mts && grep -q "test-integration:" Makefile && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - vitest.integration.config.mts exists and contains `environment: 'node'`
    - vitest.integration.config.mts contains `loadEnv`
    - vitest.integration.config.mts contains `include: ['test/integration/tenant-provisioning.test.ts']`
    - vitest.integration.config.mts contains `'server-only': path.resolve`
    - Makefile contains `test-integration:`
    - Makefile contains `npx vitest run --config vitest.integration.config.mts`
    - Makefile .PHONY block contains `test-integration`
    - vitest.config.mts is unchanged (`git diff --quiet vitest.config.mts` exits 0)
  </acceptance_criteria>
  <done>A separate Node-environment integration config exists with .env.local loading, the unit config is untouched, and `make test-integration` invokes it.</done>
</task>

<task type="auto">
  <name>Task 2: Write tenant-provisioning.test.ts integration suite</name>
  <files>test/integration/tenant-provisioning.test.ts</files>
  <read_first>
    - lib/tenant/registrationService.ts (BYODBRegistrationService.register signature after 04-01)
    - lib/tenant/vaultStore.ts (SupabaseVaultStore constructor — confirm it accepts an injected SupabaseClient)
    - lib/tenant/probe.ts and lib/tenant/probeDriver.ts (ConnectivityProbe interface — use a stub probe returning { reachable: true, ownsSchema: true } since spinning up a real BYODB target is out of scope; the integration value is the real Vault + real RLS)
    - supabase/migrations/20260609120000_create_tenants.sql (tenants columns + RLS policy names)
    - supabase/migrations/20260609130000_byodb_vault_rpc.sql (store_byodb_secret / delete_byodb_secret RPC names + how secrets are read back)
    - .planning/phases/04-rollback-error-recovery/04-RESEARCH.md (Pattern 4 setup/teardown; Pitfalls 2,3,4 — env vars, service does NOT write tenant DB row, RLS needs authenticated client)
  </read_first>
  <action>
    Create test/integration/tenant-provisioning.test.ts. Build a service-role client directly: `const admin = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.SUPABASE_SERVICE_ROLE_KEY!)` (do NOT import lib/supabase/service.ts — it is server-only and constructs at call time anyway). Build an anon client for RLS assertions: `const anon = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!)`.

    Setup/teardown (Claude's Discretion per CONTEXT.md — use this approach):
    - beforeAll: create two ephemeral test users via `admin.auth.admin.createUser({ email, password, email_confirm: true })` → userA, userB. Insert one tenant row per user via admin client: `admin.from('tenants').insert({ owner_id, state: 'Provisioning' }).select('id').single()` → tenantA, tenantB.
    - afterEach: delete any vault secrets created during the test by id (track returned secretIds), and reset tenantA back to state 'Provisioning' via admin update.
    - afterAll: delete both tenant rows (admin.from('tenants').delete().eq('id', ...)) and delete both auth users (admin.auth.admin.deleteUser(id)).

    Write these test cases (use a stub probe `{ probe: async () => ({ reachable: true, ownsSchema: true }) }` and the real `new SupabaseVaultStore(admin)`):
    - TEST-04 happy path: `svc.register(tenantA, VALID_INPUT)` returns state 'Active' and a non-empty secretId; assert the secret EXISTS in Vault by reading it back via the service-role client / RPC (per the read-back path in 20260609130000_byodb_vault_rpc.sql). Track secretId for cleanup.
    - ROLLBACK-03 against real Vault: force a post-store failure by injecting a probe that passes but spying transition to throw (or wrap the service so the transition step fails); after `register` rejects, assert the Vault secret for that attempt does NOT exist (read-back returns null/zero rows). This proves no orphaned credential remains. IMPORTANT: per Pitfall 3 the service does NOT write the tenants DB row — assert on the Vault secret lifecycle and the thrown error, NOT on tenants.vault_secret_id.
    - SEC-02/TEST-03 RLS isolation: sign in as userA via `anon.auth.signInWithPassword(...)`, create a client carrying userA's access token in the Authorization header; query tenantB's id and assert `data` has length 0 (RLS blocks cross-tenant read). Then assert userA CAN read tenantA (length 1).
    - Vault lifecycle: explicitly call `vault.store(...)` then `vault.delete(secretId)` via SupabaseVaultStore and assert the secret is gone after delete.

    Define VALID_INPUT inline as a structured postgres credential object matching BYODBCredentialInput (engine:'postgres', host:'db.example.com', port:5432, database:'mydb', user:'alice', password:'hunter2'). If the read-back of a Vault secret requires a specific RPC, read its name and signature from 20260609130000_byodb_vault_rpc.sql and call it via `admin.rpc('<name>', { ... })`.

    Add a top-of-file guard so the suite fails clearly if env is missing: at module top, if `!process.env.NEXT_PUBLIC_SUPABASE_URL` throw a descriptive Error instructing the runner to `make db-start` and ensure .env.local is populated.
  </action>
  <verify>
    <automated>test -f test/integration/tenant-provisioning.test.ts && grep -q "BYODBRegistrationService" test/integration/tenant-provisioning.test.ts && grep -q "SupabaseVaultStore" test/integration/tenant-provisioning.test.ts && grep -q "signInWithPassword" test/integration/tenant-provisioning.test.ts && echo STRUCTURE_OK</automated>
  </verify>
  <acceptance_criteria>
    - test/integration/tenant-provisioning.test.ts exists
    - contains `import { createClient } from '@supabase/supabase-js'`
    - contains `BYODBRegistrationService` and `SupabaseVaultStore`
    - does NOT contain `from '@/lib/supabase/service'` (server-only must not be imported)
    - contains `auth.admin.createUser` and `auth.admin.deleteUser`
    - contains `signInWithPassword` (RLS authenticated-client assertion)
    - contains at least one `toHaveLength(0)` (cross-tenant RLS zero-rows assertion)
    - `grep -c "it(" test/integration/tenant-provisioning.test.ts` returns >= 4
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Integration suite covers happy-path Vault store, ROLLBACK-03 orphan-secret deletion against real Vault, RLS cross-tenant isolation via authenticated client, and explicit Vault store/delete lifecycle, with ephemeral user + tenant setup/teardown.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify integration suite passes against local Supabase</name>
  <action>Pause for the user to run the integration suite against a running local Supabase (Docker). Claude cannot guarantee a Docker daemon or running Supabase instance, so this verification is human-driven after Tasks 1-2 complete.</action>
  <what-built>
    A Node-environment integration suite (test/integration/tenant-provisioning.test.ts) plus its config (vitest.integration.config.mts) and a `make test-integration` target. These exercise the real local Supabase Vault RPCs and RLS policies — which require Docker + a running local Supabase instance that the automated harness cannot guarantee.
  </what-built>
  <how-to-verify>
    1. Ensure Docker is running.
    2. Run `make db-start` to start local Supabase (wait for it to report ready).
    3. Confirm .env.local contains NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, and SUPABASE_SERVICE_ROLE_KEY (run `make db-status` to print local values if needed).
    4. Run `make migrate` to ensure all migrations are applied.
    5. Run `make test-integration` — expect ALL integration tests to pass (happy path, ROLLBACK-03 orphan cleanup, RLS isolation, Vault lifecycle).
    6. Confirm no leftover test rows: `npx supabase status` is fine; the suite cleans up after itself.
  </how-to-verify>
  <verify>
    <automated>MANUAL — run `make db-start && make migrate && make test-integration`; all integration tests must pass (Docker required, cannot run in headless harness)</automated>
  </verify>
  <resume-signal>Type "approved" if all integration tests pass, or paste the failure output to revise.</resume-signal>
</task>

</tasks>

<verification>
- `grep -q "environment: 'node'" vitest.integration.config.mts` — config in Node env
- `make test-integration` — all integration tests pass against local Supabase (manual, Docker required)
- `npx tsc --noEmit` — type-clean
- `git diff --quiet vitest.config.mts` — unit config untouched
</verification>

<success_criteria>
- Integration tests run in Node env against real local Supabase, loading env from .env.local
- Happy path proves a credential is stored in the real Vault; ROLLBACK-03 proves the secret is deleted on rollback (no orphan)
- RLS proves Tenant A cannot read Tenant B's tenant row via an authenticated client
- `make test-integration` is the documented local-only command; CI `make test` (unit) remains unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/04-rollback-error-recovery/04-02-SUMMARY.md`
</output>
