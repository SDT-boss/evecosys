---
phase: 04-board-tenant-settings
plan: 07
type: execute
wave: 6
depends_on:
  - 04-05
files_modified:
  - app/api/board/settings/byodb/route.ts
  - app/api/board/settings/users/reset-password/route.ts
  - test/unit/api/board/settings/byodb.test.ts
autonomous: true
gap_closure: true
requirements:
  - BSET-02
  - BSET-03

must_haves:
  truths:
    - "When register() throws ConnectivityError or CredentialValidationError and the route itself caused the Registered→Provisioning transition, the tenant state is rolled back to Registered before returning 400"
    - "A tenant whose BYODB registration fails with ConnectivityError can immediately retry — it is not permanently stuck in Provisioning"
    - "The reset-password route returns 501 Not Implemented (not 200) so MemberTable cannot show a false 'Reset email sent' message"
    - "The TODO comment in reset-password/route.ts is removed — the debt marker gate passes"
  artifacts:
    - path: "app/api/board/settings/byodb/route.ts"
      provides: "BYODB route with compensating rollback in ConnectivityError and CredentialValidationError catch branches"
      contains: "initialState"
    - path: "app/api/board/settings/users/reset-password/route.ts"
      provides: "501 Not Implemented stub — no false success response"
    - path: "test/unit/api/board/settings/byodb.test.ts"
      provides: "Unit test verifying Provisioning→Registered rollback on ConnectivityError"
  key_links:
    - from: "app/api/board/settings/byodb/route.ts"
      to: "admin.from('tenants').update({ state: 'Registered' })"
      via: "ConnectivityError catch branch rollback"
      pattern: "state.*Registered.*rollback|rollback.*Registered"
    - from: "test/unit/api/board/settings/byodb.test.ts"
      to: "POST /api/board/settings/byodb"
      via: "ConnectivityError rollback test"
      pattern: "rolls back.*Registered|Registered.*rollback"
---

<objective>
Close CR-01 and CR-02. CR-01 adds a compensating rollback in the BYODB route so ConnectivityError and CredentialValidationError failures restore the tenant state to Registered (only when the route itself caused the Registered→Provisioning write). CR-02 changes the reset-password stub to return 501 Not Implemented, eliminating the false "Reset email sent" UX and the unreferenced TODO debt marker.

Purpose: Prevent tenants from being permanently stuck in Provisioning on transient connectivity failures (CR-01), and prevent the board member from being misled into believing a password reset email was sent when no operation occurred (CR-02).

Output: Corrected byodb route with rollback logic, new unit test for the rollback path, and a 501 reset-password route with no TODO debt marker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-board-tenant-settings/04-CONTEXT.md
@.planning/phases/04-board-tenant-settings/04-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-01 — Add compensating rollback in byodb route + unit test for rollback path</name>
  <files>
    app/api/board/settings/byodb/route.ts,
    test/unit/api/board/settings/byodb.test.ts
  </files>
  <read_first>
    - app/api/board/settings/byodb/route.ts (full current file — understand lines 53-95 where state transition and catch branches live)
    - test/unit/api/board/settings/byodb.test.ts (full current file — understand existing mock setup, adminUpdateChain helper, and the existing ConnectivityError test at line 170 to know exactly which test to update and which new test to add)
  </read_first>
  <behavior>
    - Test: When tenant initial state is Registered and register() throws ConnectivityError, the route calls admin.from('tenants').update({ state: 'Registered' }) with .eq('id', tenant.id) BEFORE returning 400
    - Test: When tenant initial state is Provisioning (route did NOT cause the transition) and register() throws ConnectivityError, the route does NOT roll back — it returns 400 without calling any update
    - Existing test "returns 400 when register() throws ConnectivityError" uses tenant state=Provisioning and no rollback — it should continue to pass unchanged (the route should NOT roll back when it did not cause the transition)
  </behavior>
  <action>
    Write the rollback test FIRST (RED phase), then implement the fix (GREEN phase).

    RED — Add one new test to `test/unit/api/board/settings/byodb.test.ts`:

    After the existing "returns 400 when register() throws ConnectivityError" test (which uses state=Provisioning), add a new test:

    ```
    it('rolls back tenant state to Registered when register() throws ConnectivityError after Registered→Provisioning transition', async () => {
      const { POST } = await import('@/app/api/board/settings/byodb/route')
      mockGetUser.mockResolvedValue({ data: { user: { id: 'u1' } } })

      let callCount = 0
      mockFrom.mockImplementation(() => {
        callCount++
        if (callCount === 1) return profileChain('board')
        return tenantChain({ id: 'tenant-1', state: 'Registered', owner_id: 'u1', name: 'Acme', created_at: '', updated_at: '' })
      })

      const provisioningUpdateEq = vi.fn().mockResolvedValue({ error: null })
      const rollbackUpdateEq = vi.fn().mockResolvedValue({ error: null })
      const updateFn = vi.fn()
        .mockReturnValueOnce({ eq: provisioningUpdateEq })   // first update: Registered → Provisioning
        .mockReturnValueOnce({ eq: rollbackUpdateEq })        // second update: rollback → Registered

      mockAdminFrom.mockReturnValue({ update: updateFn })
      mockRegister.mockRejectedValue(new ConnectivityError('host unreachable'))

      const res = await POST(makeRequest(validInput))

      expect(res.status).toBe(400)
      // First update must set state to Provisioning
      expect(updateFn).toHaveBeenNthCalledWith(1, { state: 'Provisioning' })
      // Second update must roll back to Registered
      expect(updateFn).toHaveBeenNthCalledWith(2, { state: 'Registered' })
      expect(rollbackUpdateEq).toHaveBeenCalledWith('id', 'tenant-1')
    })
    ```

    Also add a companion test for CredentialValidationError rollback (same structure, swap error class):
    ```
    it('rolls back tenant state to Registered when register() throws CredentialValidationError after Registered→Provisioning transition', async () => {
      // ... identical setup to above but mockRegister throws CredentialValidationError('bad creds')
    })
    ```

    Run `make test` — these tests MUST fail (RED) because the route has no rollback yet.

    GREEN — Update `app/api/board/settings/byodb/route.ts`:

    At the top of the POST handler, before the state transition block, capture the initial state:
    ```
    const initialState = tenant.state
    ```

    In the try/catch block, update BOTH the ConnectivityError catch branch AND the CredentialValidationError catch branch to add a rollback before returning 400. The rollback only fires if the route itself caused the transition (i.e., `initialState === 'Registered'`):

    ```typescript
    } catch (err) {
      if (err instanceof CredentialValidationError) {
        if (initialState === 'Registered') {
          await admin.from('tenants').update({ state: 'Registered' }).eq('id', tenant.id)
        }
        return NextResponse.json({ error: (err as Error).message }, { status: 400 })
      }
      if (err instanceof ConnectivityError) {
        if (initialState === 'Registered') {
          await admin.from('tenants').update({ state: 'Registered' }).eq('id', tenant.id)
        }
        return NextResponse.json({ error: (err as Error).message }, { status: 400 })
      }
      throw err
    }
    ```

    The `admin` variable is already in scope (created before the state transition block). The `tenant.id` is also in scope.

    Do NOT change the existing "returns 400 when register() throws ConnectivityError" test — it uses state=Provisioning and the rollback guard `initialState === 'Registered'` ensures no rollback is called in that path, so that test continues to pass.

    Run `make test` — all tests including the two new ones MUST pass (GREEN).
  </action>
  <verify>
    <automated>cd /Users/shannendorothee/Projects/evecosys && make test 2>&1 | grep -E "byodb|PASS|FAIL|✓|✗" | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with all byodb tests passing
    - `grep -n "initialState" app/api/board/settings/byodb/route.ts` returns a match (the captured initial state variable)
    - `grep -n "state.*Registered.*rollback\|rollback\|update.*state.*Registered" app/api/board/settings/byodb/route.ts` confirms the rollback update call exists in the catch branch
    - `grep -c "rolls back tenant state to Registered" test/unit/api/board/settings/byodb.test.ts` returns 2 (the ConnectivityError and CredentialValidationError rollback tests)
    - The existing test "returns 400 when register() throws ConnectivityError" (state=Provisioning) still passes — no spurious rollback in that path
    - `grep -n "initialState === 'Registered'" app/api/board/settings/byodb/route.ts` returns 2 matches (one guard per catch branch)
  </acceptance_criteria>
  <done>byodb route rolls back tenant state to Registered when register() throws ConnectivityError or CredentialValidationError and the route itself caused the Registered→Provisioning transition. Unit tests verify both rollback paths and the no-rollback path.</done>
</task>

<task type="auto">
  <name>Task 2: CR-02 — Change reset-password stub to return 501 Not Implemented, remove TODO debt marker</name>
  <files>app/api/board/settings/users/reset-password/route.ts</files>
  <read_first>
    - app/api/board/settings/users/reset-password/route.ts (full current file — line 22 is the TODO, line 23 returns { ok: true })
    - components/board/settings/MemberTable.tsx (confirm how the 200 vs non-200 response is handled — the component currently treats any 2xx as "Reset email sent"; a non-2xx will fall into its error state)
  </read_first>
  <action>
    Edit `app/api/board/settings/users/reset-password/route.ts` to return 501 Not Implemented instead of 200, and remove the unreferenced TODO comment.

    Specific changes:
    1. Remove the `// TODO: implement actual password reset email via admin.auth.admin.generateLink('recovery', { email })` comment at line 22.
    2. Remove the `// eslint-disable-next-line @typescript-eslint/no-unused-vars` directive and the `const _body = await req.json()` body-parse call (the 501 stub does not need to parse the request body).
    3. Replace `return NextResponse.json({ ok: true })` with:
       ```
       return NextResponse.json({ error: 'Password reset not yet implemented' }, { status: 501 })
       ```
    4. Update the file-level JSDoc comment (the block starting with `// Stub: force password reset endpoint`) to reflect the 501 behaviour — replace the last sentence with: `Returns 501 Not Implemented so the client shows an error state rather than false success.`

    The auth guard (lines 8-17) stays exactly as-is — the route still requires an authenticated board member before returning 501, which prevents information leakage to unauthenticated callers.

    After the edit the file must contain NO occurrence of the string `TODO` and NO occurrence of `ok: true`.
  </action>
  <verify>
    <automated>grep -c "TODO\|ok: true" /Users/shannendorothee/Projects/evecosys/app/api/board/settings/users/reset-password/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TODO" app/api/board/settings/users/reset-password/route.ts` returns 0
    - `grep -c "ok: true" app/api/board/settings/users/reset-password/route.ts` returns 0
    - `grep -n "501" app/api/board/settings/users/reset-password/route.ts` returns a match (the status code)
    - `grep -n "Password reset not yet implemented" app/api/board/settings/users/reset-password/route.ts` returns a match (the error body)
    - Auth guard (getUser + role check) is still present — `grep -c "getUser\|profile?.role !== 'board'" app/api/board/settings/users/reset-password/route.ts` returns 2
    - `make typecheck` exits 0
    - `make test` exits 0
  </acceptance_criteria>
  <done>reset-password route returns 501 Not Implemented with a JSON error body. No TODO debt marker remains. MemberTable will receive a non-2xx response and display an error state instead of "Reset email sent".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BYODB route → tenants.state in DB | Route writes Provisioning before calling register(); failure must roll back to avoid permanent state corruption |
| reset-password route → MemberTable UX | Route response code determines whether the UI shows success or error — a false 200 is a trust boundary violation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-CR01-01 | Tampering | byodb/route.ts — tenant state machine | mitigate | Track `initialState` at route entry; rollback `Provisioning → Registered` in catch branches only when `initialState === 'Registered'`; unit tests assert rollback call is made with correct parameters |
| T-04-CR01-02 | Denial of Service | byodb/route.ts — stuck Provisioning state | mitigate | Compensating update restores Registered state so future registration attempts proceed through the full state machine path rather than being silently skipped |
| T-04-CR02-01 | Spoofing | reset-password/route.ts — false 200 ok:true | mitigate | Return 501 Not Implemented; client error handling in MemberTable already checks `res.ok` (non-2xx) — this change ensures the false success path cannot be triggered |
| T-04-CR-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After both tasks complete:

1. `make test` exits 0 — all byodb tests pass including the two new rollback tests
2. `grep -c "initialState" app/api/board/settings/byodb/route.ts` returns at least 1
3. `grep -c "initialState === 'Registered'" app/api/board/settings/byodb/route.ts` returns 2 (one guard per catch branch)
4. `grep -c "rolls back tenant state" test/unit/api/board/settings/byodb.test.ts` returns 2
5. `grep -c "TODO" app/api/board/settings/users/reset-password/route.ts` returns 0
6. `grep -c "501" app/api/board/settings/users/reset-password/route.ts` returns at least 1
7. `make typecheck` exits 0
</verification>

<success_criteria>
- byodb route has compensating rollback: ConnectivityError + CredentialValidationError catch branches restore tenant state to Registered when initialState === 'Registered'
- Two new unit tests verify the rollback path (one per error class); existing tests continue to pass
- reset-password route returns 501 Not Implemented — no TODO debt marker, no false ok:true response
- `make typecheck` and `make test` both exit 0
- CR-01 and CR-02 are fully closed
</success_criteria>

<output>
Create `.planning/phases/04-board-tenant-settings/04-07-SUMMARY.md` when done.
</output>
