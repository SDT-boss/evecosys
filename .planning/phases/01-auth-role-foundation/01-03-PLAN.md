---
phase: 01-auth-role-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - e2e/helpers/auth.helpers.ts
  - e2e/global-setup.ts
  - e2e/tests/auth-guards/role-isolation.spec.ts
autonomous: true
requirements: [AUTH-03]
gap_closure: true

must_haves:
  truths:
    - "A board user with no tenant row is denied access to /board/settings and redirected to /login — exercised by an automated E2E test"
    - "The board_no_tenant test user is created in Supabase during global setup and never gets a tenant row"
    - "The new E2E test passes when make e2e runs against a local Supabase instance"
  artifacts:
    - path: "e2e/helpers/auth.helpers.ts"
      provides: "board_no_tenant entry in TEST_USERS, AUTH_STATE_PATH, and destination maps"
      contains: "board_no_tenant"
    - path: "e2e/global-setup.ts"
      provides: "ensureTestUser call and storageState generation for board_no_tenant"
      contains: "board_no_tenant"
    - path: "e2e/tests/auth-guards/role-isolation.spec.ts"
      provides: "E2E test: board member WITHOUT tenant cannot access /board/settings"
      contains: "board-no-tenant.json"
  key_links:
    - from: "e2e/global-setup.ts ensureTestUser('board_no_tenant')"
      to: "e2e/helpers/auth.helpers.ts TEST_USERS.board_no_tenant"
      via: "TEST_USERS[role] lookup"
      pattern: "board_no_tenant"
    - from: "role-isolation.spec.ts storageState"
      to: "e2e/.auth/board-no-tenant.json"
      via: "Playwright storageState option"
      pattern: "board-no-tenant\\.json"
---

<objective>
Closes the SC-2 E2E testing gap: the negative case for the board/settings guard (board user with no tenant) has no automated test. The code path is implemented and verified in code, but no test exercises it.

Purpose: AUTH-03 requires the board tenant settings route to enforce that a board user without a tenant row is redirected to /login. This plan adds the test user infrastructure and the E2E assertion for that code branch.

Output:
- board_no_tenant test user wired into the E2E helper, global setup, and spec file
- One new test.describe block asserting GET /board/settings → /login for a no-tenant board user
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add board_no_tenant user to E2E auth helpers</name>

  <files>e2e/helpers/auth.helpers.ts</files>

  <read_first>
    e2e/helpers/auth.helpers.ts — read the full file before editing; understand the shape of TEST_USERS, AUTH_STATE_PATH, loginViaUI destinations, and loginViaAPI destinations. All four of these objects must gain a board_no_tenant entry.
  </read_first>

  <action>
Add a fifth entry to TEST_USERS immediately after the board entry:

  board_no_tenant: {
    email: process.env.E2E_BOARD_NO_TENANT_EMAIL ?? 'e2e-board-no-tenant@evecosys-test.com',
    password: process.env.E2E_TEST_PASSWORD ?? 'TestPassword123!',
    name: 'E2E Board No Tenant',
    role: 'board' as const,
  },

The role is 'board' (not a new role). This user will never have a tenant row in public.tenants because createTestUser only writes to auth.users and public.users — it does not insert into public.tenants. No special cleanup is needed.

In AUTH_STATE_PATH (Record&lt;RoleKey, string&gt;), add:
  board_no_tenant: 'e2e/.auth/board-no-tenant.json',

In loginViaUI, inside the destinations record, add:
  board_no_tenant: '/board',

Board users land on /board after login — this user can reach /board but not /board/settings.

In loginViaAPI, inside the destinations record, add:
  board_no_tenant: '/board',

These are the only four places in auth.helpers.ts that must be updated. Do not modify any other logic. RoleKey is derived as `keyof typeof TEST_USERS` so TypeScript will automatically include board_no_tenant once it is added to TEST_USERS.
  </action>

  <verify>
    <automated>grep -c "board_no_tenant" e2e/helpers/auth.helpers.ts</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "board_no_tenant" e2e/helpers/auth.helpers.ts returns 5 (one in TEST_USERS definition, one in AUTH_STATE_PATH, one in loginViaUI destinations, one in loginViaAPI destinations, one in the email env var line — minimum 4 occurrences; 5 if env var name counts separately)
    - The string 'e2e-board-no-tenant@evecosys-test.com' appears as the default email fallback
    - The string 'e2e/.auth/board-no-tenant.json' appears in AUTH_STATE_PATH
    - Both destination maps contain board_no_tenant: '/board'
    - npx tsc --noEmit exits 0 with no output (no TypeScript errors)
  </acceptance_criteria>

  <done>board_no_tenant is present in all four required locations in auth.helpers.ts and TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Wire board_no_tenant into global setup and add the E2E test</name>

  <files>e2e/global-setup.ts, e2e/tests/auth-guards/role-isolation.spec.ts</files>

  <read_first>
    e2e/global-setup.ts — read the full file. Note the Promise.all at line 64 (ensureTestUser calls) and the storageState generation tuple at line 74. Both need board_no_tenant added.

    e2e/tests/auth-guards/role-isolation.spec.ts — read the full file. The new test.describe block goes after the existing "Manager — cannot access /board/settings" describe block (currently the last block in the file, ending at line 159).

    e2e/helpers/supabase.admin.ts — read lines 47-77 (createTestUser function signature). Confirm the role parameter accepts 'board'. This confirms board_no_tenant with role: 'board' will be handled correctly by ensureTestUser without any changes to supabase.admin.ts.
  </read_first>

  <action>
In e2e/global-setup.ts:

1. In the Promise.all at the ensureTestUser block (currently lines 64-70), add:
     ensureTestUser('board_no_tenant'),
   Place it after ensureTestUser('platform_admin') and before ensureUser(FORCED_RESET_USER).

2. In the storageState generation Promise.all, the cast tuple is:
     (['manager', 'driver', 'board', 'platform_admin'] as const)
   Change this to:
     (['manager', 'driver', 'board', 'board_no_tenant', 'platform_admin'] as const)
   Order does not matter for correctness; putting board_no_tenant after board is logical.

In e2e/tests/auth-guards/role-isolation.spec.ts:

Add a new test.describe block at the end of the file (after the "Manager — cannot access /board/settings" block):

  // ─── Board member WITHOUT tenant accessing /board/settings ───────────────────

  test.describe('Board member WITHOUT tenant — cannot access /board/settings', () => {
    test.use({ storageState: 'e2e/.auth/board-no-tenant.json' })

    test('board user with no tenant GET /board/settings → /login', async ({ page }) => {
      await page.goto('/board/settings')
      await expect(page).toHaveURL('/login', { timeout: 10_000 })
    })
  })

This exercises the second redirect branch in app/(dashboard)/board/settings/layout.tsx — the path that fires when the Supabase query for a tenant with owner_id = user.id returns null (no tenant row exists for this user).

Do not modify any other part of global-setup.ts or role-isolation.spec.ts.
  </action>

  <verify>
    <automated>grep -c "board_no_tenant" e2e/global-setup.ts && grep -c "board-no-tenant" e2e/tests/auth-guards/role-isolation.spec.ts</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "board_no_tenant" e2e/global-setup.ts returns 2 (one in ensureTestUser call, one in the storageState tuple)
    - grep -c "board-no-tenant" e2e/tests/auth-guards/role-isolation.spec.ts returns 2 (one in the storageState path string, one in the test name or description — minimum 1, ideally 2)
    - The storageState path string 'e2e/.auth/board-no-tenant.json' appears exactly once in role-isolation.spec.ts
    - The new test.describe block title is exactly: 'Board member WITHOUT tenant — cannot access /board/settings'
    - The new test asserts: await expect(page).toHaveURL('/login', { timeout: 10_000 }) after page.goto('/board/settings')
    - npx tsc --noEmit exits 0 with no output
    - npx playwright test e2e/tests/auth-guards/role-isolation.spec.ts --grep "board user with no tenant" passes (requires running local Supabase + Next.js app; skip in CI static check)
  </acceptance_criteria>

  <done>global-setup.ts creates and authenticates the board_no_tenant user on startup; role-isolation.spec.ts contains an E2E test asserting that a board user with no tenant row is redirected from /board/settings to /login.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E test user → Supabase auth | board_no_tenant authenticates via the same REST API token flow as other test users |
| board_no_tenant user → public.tenants | No row must ever be inserted for this user; the guard code relies on this absence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | board_no_tenant user in public.tenants | accept | createTestUser does not insert into public.tenants; no setup code creates a tenant row for this user. If a test elsewhere creates a tenant with this user as owner_id, the E2E test will incorrectly pass — no mitigation needed at this scope (test isolation is a separate concern) |
| T-01-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After both tasks complete:

1. TypeScript: `npx tsc --noEmit` exits 0
2. Helpers wired: `grep -c "board_no_tenant" e2e/helpers/auth.helpers.ts` returns >= 4
3. Global setup wired: `grep -c "board_no_tenant" e2e/global-setup.ts` returns 2
4. Spec has new test: `grep -c "board-no-tenant" e2e/tests/auth-guards/role-isolation.spec.ts` returns >= 1
5. Spec has correct assertion: `grep "toHaveURL('/login'" e2e/tests/auth-guards/role-isolation.spec.ts | wc -l` shows the existing login assertions plus the new one
6. E2E (requires live app): `make e2e` exits 0 — all role-isolation tests pass including the new board-no-tenant test
</verification>

<success_criteria>
- A board user with no tenant row is denied /board/settings and redirected to /login in an automated E2E test
- The test infrastructure (test user, auth state file, global setup) supports the new test user without touching any production code
- TypeScript compiles cleanly across all modified E2E files
- No existing E2E tests are broken by the changes
- AUTH-03 is fully covered: the board-with-tenant positive case (already passing) and the board-without-tenant negative case (added by this plan) are both exercised
</success_criteria>

<output>
Create `.planning/phases/01-auth-role-foundation/01-03-SUMMARY.md` when done
</output>
