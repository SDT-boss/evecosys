Run tests with Vitest (install dev dependencies first):

```bash
npm install
npm run test
```

Tests use jsdom and @testing-library/react. If you run into issues, ensure `vitest` and testing libraries are installed.
